# PR-6.75 (C0-full + C1) plan driver log — session c0c1-plan

Role: PLANNING-ONLY driver. Report to team-lead. Deliverable: signed-off plan at
`.planning/combo-detection/PR-6.75-C0FULL-C1-PLAN.md` (main workdir). NO engine edits anywhere.
pr65-wt is STRICTLY READ-ONLY (pr65-impl actively writing there).

USER DIRECTIVES (via team-lead, 2026-07-02):
- Designation is PR-6.75 (not PR-6.6). Predecessor PR-6.5 (feat/combo-pr6.5-growing-cascade).
- ENTIRE plan/review pipeline on FABLE (claude-fable-5) @ XHIGH effort: every /engine-planner,
  every /review-engine-plan, every reviser sub-agent. Driver (me) stays as spawned.

## Mission (close the PR-6.5 inc2a deferral)
Land C0-full (replace fail-open allowlist `value_contains_trigger_event_context_ref`
triggers.rs:~3358) + C1 (apply classifier at same-event short-circuit
`trigger_events_match_for_ordering` :~3413, latent CR 603.3b bug) — both ungated — with
ZERO printed-card behavior change. Blocked in inc2a because inc1 walker is fail-closed-COARSE:
~46 Effect kinds → wholesale `Axes::CONSERVATIVE` on sibling/event axes, coarser than the
fail-OPEN allowlist on legacy UNGATED paths.

Plan must cover:
1. PRECISION PASS over ~46 CONSERVATIVE effect kinds (sibling/event axes) → match/beat allowlist.
2. EventSource/zone CRUX: Nested Shambler #4269 co-departing death-tokens rely on allowlist NOT
   flagging EventSource on same-departure-batch path. Rules-correct treatment (CR 603.3b vs
   proven order-independence of `zone_changes_are_same_departure_batch`).
3. C1 landing: apply at same-event short-circuit; synthetic order-dep group prompts, printed parity.
4. C0-full landing: replace/retire allowlist + delete `zone_changes_are_same_departure_batch` ONLY
   IF genuinely dead (inc2a C2-gating likely made it live-on-OFF-path → likely STAYS).
5. VERIFICATION MATRIX: parity corpus (2 broken tests + Nested Shambler #4269 + Rubblebelt/Orcish)
   + synthetic C1 discriminator + mechanical allowlist-parity proof (diff both classifiers over all
   printed-card trigger groups).
6. Series bookkeeping: PR-6.6, predecessor link, rebase onto FINAL PR-6.5 (inc2b moves anchors).

CONSTRAINT: do NOT change axis-3 (projected-resource) classifications — precise, consumed by inc2b.

## Key facts (from PR-6.5-PLAN.md + PR-6.25-DEFERRED-FINDINGS.md, read 2026-07-02)
- Walker `crates/engine/src/game/ability_scan.rs` @ pr65-wt 47adf7fc1. 3 axes:
  (1) event-context read, (2) sibling-mutable read/write conflict, (3) projected-resource read.
- inc1 = 57c4b7924 (walker). inc2a = 47adf7fc1 (C2-only gated auto-resolve).
- C1 same-event bug: `trigger_events_match_for_ordering` returns true on
  `first.trigger_event == candidate.trigger_event` BEFORE any soundness gate (triggers.rs:~3413).
  Measured 0 printed-card reachability → synthetic discriminator only.
- Case A UNSOUND counterexample (PR-6.25 §1): "+1/+1 on each creature you control" + "draw if power>=6",
  two byte-identical copies, order-observable. Sibling-write + non-linear self-read; NOT event-context.
- Danger cards for C2 read-set gate: Rubblebelt Rioters, Orcish Siegemaster (self-pump reading
  board aggregate a sibling mutates). Must keep PROMPTING.
- Allowlist escapees (event-context axis, currently MISSED by 12-entry allowlist): ObjectScope::EventSource,
  TargetFilter::TriggeringSourceController, ParentTargetSlot, QuantityRef::TimesCostPaidThisResolution,
  CastManaObjectScope::TriggeringSpell, RestrictionPlayerScope::ParentTargetedPlayer.
- inc2a broke: `multiple_dynamic_demonstrate...`, `granted_replicate_paid_twice...` (C1 over-prompt
  copy/token/pump printed cards, auto-order today).

## WALKER RECON (walker-recon agent, 2026-07-02, ability_scan.rs @ 47adf7fc1, 3102 lines)
- `Axes` = 3-bool struct :68 {event, sibling, projected}; `NONE` :84, `CONSERVATIVE` :91 = ALL-TRUE.
  Single traversal, OR-accumulated (`Axes::or` :97). NO partial-conservative arms exist.
- **42 wholesale-CONSERVATIVE arms** (not ~46): **35 Effect::** — Pump:324, Counter:345, Token:351,
  PumpAll:400, ChangeZone:430, ChangeZoneAll:431, Vote:523, SeparateIntoPiles:524, CopySpell:530,
  EpicCopy:531, CopyTokenOf:540, BecomeCopy:589, Animate:650, ReturnAsAura:651, GenericEffect:653,
  Mana:655, SearchLibrary:684, RevealFromHand:705, Choose:727, CreateDelayedTrigger:793,
  AddTargetReplacement:794, AddRestriction:795, CreateEmblem:820, PayCost:821, CastFromZone:822,
  CreateDamageReplacement:849, RollDie:871, FlipCoin:872, FlipCoins:873, FlipCoinUntilLose:874,
  GrantCastingPermission:928, ExileFromTopUntil:985, PutAtLibraryPosition:1022, Conjure:1232,
  ChooseOneOf:1244. **+7 non-Effect**: QuantityRef::DistinctCardTypes:1442, ManaSpentToCast:1683,
  AbilityCondition::RevealedHasCardType:1791, TargetFilter::Typed:1957,
  TriggerCondition::AttackersDeclaredCount:2336, StaticCondition::UnlessPay:2484,
  ReplacementCondition::UnlessControlsOtherLeq:2638. **+2 non-arm**: mode_abilities non-empty →
  CONSERVATIVE :221-223; unless_pay.is_some() → projected-only :202-204.
- **AXIS-3 COUPLING (the hard invariant)**: CONSERVATIVE arms have projected=true (all-true const).
  Precision pass MUST rewrite arms as `Axes { event: <precise>, sibling: <precise>, projected: true }`
  (start acc there, OR in descended sub-scans) — that pins axis-3 byte-identical. Axis-3 readout fns
  all #[allow(dead_code)] (6 of them, inc2b consumers); axes 1/2 fns LIVE:
  ability_uses_event_context:2809, ability_reads_sibling_mutable:2817 (root: resolved_ability_axes:115,
  no-`..` destructure :116-162).
- Escapees: 4 precise event-only arms (EventSource:2091, TriggeringSourceController:2031,
  ParentTargetSlot:2041, TimesCostPaidThisResolution:1678); 2 covered ONLY via CONSERVATIVE carriers
  (CastManaObjectScope::TriggeringSpell via ManaSpentToCast:1683; RestrictionPlayerScope::
  ParentTargetedPlayer via AddRestriction/AddTargetReplacement:794-795 — scope enums NOT traversed).
  Precision pass on those carriers must keep event=true or add scope traversal.
- Rubblebelt/Orcish class: today classified via Pump→CONSERVATIVE (event+sibling both true).
  Precise Pump descent ⇒ sibling-only via Aggregate:1418/Power:1336 (sibling:true,event:false).
  ObjectScope::Source/Recipient = NONE :2088/:2090 — sibling carried at QuantityRef layer.
- Axis-2 = single bool, no read/write-set split; conflict intersection is consumer's job.
- Zero wildcards confirmed; 18 walk fns; non-traversed-by-design: ContinuousModification,
  ManaProduction, ReplacementDefinition, FilterProp, mode_abilities, CastManaObjectScope,
  RestrictionPlayerScope (conservative at carriers, module doc :33-41).
- Tests pinning: event_context_axis_discriminates :2998-3046 (5 reachable escapees event=true).

## WIRING RECON (wiring-recon agent, 2026-07-02, triggers.rs @ 47adf7fc1 — CURRENT line anchors)
- Allowlist `value_contains_trigger_event_context_ref` :3358-3383 (doc :3350-3357). EXACTLY 12 strings.
  TWO failure modes, don't conflate: fail-OPEN at the `matches!` (unlisted string ⇒ false ⇒ batch
  permitted); fail-CLOSED at `unwrap_or(true)` (serialize Err ⇒ prompt). `ability_uses_trigger_event_context`
  :3385-3389 (serde_json::to_value tree walk).
- `trigger_events_match_for_ordering` :3416-3458, NOW 5 params (first, candidate, legacy_uses_trigger_event,
  c2_order_independent, loop_detection_on). Order: (1) same-event short-circuit :3431-3433 returns true
  BEFORE any gate (C1 target); (2) distinct-event ZoneChanged batch :3441-3449, gated on !legacy only,
  MODE-INDEPENDENT (OFF-path live); (3) C2 tail :3457 = `loop_detection_on && c2_order_independent`.
- `zone_changes_are_same_departure_batch` :3391-3414 — no /// doc; single caller :3445 (distinct-event
  path only, NOT same-event); mutual co_departed + same from/to (CR 603.10a look-back, producer
  zones.rs:779-815, sba.rs:198). REACHABLE ALL MODES ⇒ NOT deletable, STAYS.
- `group_is_order_independent(group, is_on)` :3510-3547. Computes BOTH: legacy allowlist verdict +
  `c2_order_independent = !ability_uses_event_context && !ability_reads_sibling_mutable` (walker,
  consumed ONLY by gated-C2 term). `begin_trigger_ordering` :3562-3612 reads is_on at :3590, sole
  production caller of the predicate :3592; chokepoint callers = process_triggers:3234 +
  drain_deferred_trigger_queue_unchecked:4613. `drain_order_triggers_with_identity` :3653-3669 now
  TEST-ONLY (doc :3646-3652; test callers only) — never a widening route.
- **#4269 MEASURED DISCREPANCY (crux input):** Nested Shambler parses `Token{count: Ref(Power{scope:
  Source})}` — `"Source"` NOT in allowlist (⇒ auto-resolves today via batch), and walker event axis
  does NOT flag it (Source→NONE :2088). BUT walker sibling axis DOES flag it (Power{Source} arm
  :1336 sibling:true). So the walker's TWO-AXIS conjunction regresses #4269 via SIBLING, not event.
  Code comments (:3353-3354, :3438-3439) claiming "EventSource" are WRONG for this card — plan must
  re-measure the co-departing class + correct comments. Pinning test = quantity.rs:9715-9767 (LKI
  power, CR 608.2h + 603.10a); NO ordering test pins #4269.
- Rules note for the crux: departed sources' Source-scoped reads are LKI-FROZEN (that's WHY the batch
  path is safe, per PR-6.25 §4); Case-A unsoundness needs LIVE sibling-mutable reads. Same-event
  groups: event-context reads are IDENTICAL across siblings (same event) ⇒ event axis is arguably NOT
  an order-sensitivity axis for C1; sibling axis is. Byte-identical normalized ASTs still differ as
  resolution fns via Source-binding (Case A) — identity ≠ commutativity only when self-reads are live.
  (Design considerations for planner, not pre-decisions.)
- Broken tests located: `multiple_dynamic_demonstrate_grants_enqueue_multiple_triggers` triggers.rs:
  19036-19072 (2 granted Demonstrate = same SpellCast event, CopySpell AST, counts stack entries);
  `granted_replicate_paid_twice_creates_two_copies` casting_costs.rs:15092-15143 (2 payment records ⇒
  2 same-event CopySpell triggers; drain counts SpellCopied). Both break under coarse-walker C1
  because CopySpell→CONSERVATIVE ⇒ c2_order_independent=false ⇒ prompt.
- NOTE: "StackSpell" IS allowlisted (event-context) but same-event short-circuit never consults it —
  so Demonstrate/Replicate auto-order today regardless. A C1 gate of "no event-context read" would
  break them even with a precise walker; the C1-relevant axis is sibling-conflict.
- CR grep-verified (MagicCompRules.txt lines): 603.2c:2567, 603.3b:2586, 603.3c:2588, 603.3d:2590,
  603.4:2592, 603.10a:2638.

## TEAM-LEAD VERIFICATION + REFINEMENTS (2026-07-02, relayed to planner mid-flight)
- Recon findings INDEPENDENTLY VERIFIED by team-lead (sibling-axis crux confirmed; their line refs
  differ — :1520/:2339/:2342 vs recon's :1336/:2088/:2091 — substance identical; reconcile at review).
- Refinement 1: axis-3 invariant stated as "byte-identical to inc1 output", NOT "always projected:true"
  (the latter is only the consequence for the 42 all-true arms). No axis-3 refinement in this PR even
  if refinable — flagged follow-up only.
- Refinement 2: LKI look-back argument must be PROVEN (which reads frozen at fire time vs live at
  resolution, CR 603.10a + revert_layered_characteristics_to_base trace); frozen Source-read commutes,
  LIVE board-aggregate may not — batch-path composition derived from that. #4269 needs a REAL ordering
  test.

## Status
- [x] Read PR-6.5-PLAN.md (full, 260 lines) + PR-6.25-DEFERRED-FINDINGS.md + .pr65-driver-log.md
- [x] Walker recon (above)
- [x] Wiring recon (above)
- [x] PR-6.75-RECON.md seed file written; /engine-planner spawned (fable xhigh, background)
- [x] Team-lead refinements relayed to planner mid-flight
- [x] Plan drafted (2026-07-02, fable xhigh, 286 lines §0-§8 + ledger, 14 CR) → team-lead checkpointed
- [~] Review loop (fable xhigh): R1 = GAPS 1 BLOCKER / 4 MAJOR / 5 minor (PR-6.75-REVIEW-R1.md) →
      reviser RUNNING. R1 headline: B1 LKI battlefield-RE-ENTRY hole (ObjectId stable across moves,
      zones.rs:690 — sibling graveyard→battlefield return revives a "frozen" read; constructive close
      = no toward-battlefield membership write in group write-set + hostile); M1 die_result missing
      from identity basis; M2 Chaotic Goo + Mana Crypt mis-measured (FlipCoin-only), Arcbound Worker
      has target (not gate-reachable); M3 rw-walker no-`..`-on-precise-arms mandate missing; M4
      D-profile floor can't reach zero-diff (GenericEffect/Animate/Choose/Scry-absent; Mana default
      fail-OPEN). Architecture ENDORSED. Anchor reconciliation: recon anchors CORRECT at 47adf7fc1;
      team-lead's :1520 set measured drifted working tree (+185 lines, inc2b landing).
- [x] R1 reviser COMPLETED (2026-07-02, before pause took effect on it). ALL findings closed.
      Plan now 374 lines (was 287), status header has "R1 revision (2026-07-02)" line.
      CLOSURE SUMMARY (full report in reviser task output; key points):
      • B1: freeze-invalidation ROW added to conflict predicate (writes_reentry_hazard = external
        existing-object move w/ battlefield destination OR exile origin; token-creation/self-move/
        library-origin exempt). Re-death SUBSUMED (battlefield re-entry precedes re-death; exile
        round-trip covered by exile-origin arm). Parity measured: 6 printed candidates → 4 allowlist-
        prompt today, 2 cleared by Library origin, 0 exile-origin ⇒ zero diffs. Hostile N-B2 (reviewer's
        fixture), revert-fail = drop row. §4 comment + §8 provenance claim corrected.
      • M1: die_result equality conjunct added (CR 706.2 + 603.12); measured NOT constructible today
        (all ordering-queue sites stamp None; Some sites are reflexive, bypass begin_trigger_ordering)
        ⇒ defense-in-depth, unit-pinned N-F.
      • M2: Goo/Crypt re-measured (FlipCoin-only, baseline evidence); decisive = Gutter Grime live
        CountersOn{Source} + Fruit + frozen 41. Arcbound removed; floors re-measured (batch_self
        src-read 81 = 40 targeted + 41 no-input); §8 modular claim fixed (CR 603.3d).
      • M3: all-payload-fields binding mandate added (`{..}` only on maximal-conservative RHS).
      • M4: GenericEffect payload-descent; Scry/Surveil/Animate/Choose/ChooseOneOf/CreateDelayedTrigger/
        GrantCastingPermission committed; hedges deleted; Mana closed (0 of 261 Effect-tree Mana×
        unless_pay co-occurrences; naive-grep 136 = cost-payload false positives) + N-E hostile.
        TWO measurement-forced DEVIATIONS from R1 steers: (1) DealDamage CANNOT be write-Other
        (100 printed live-src-read triggers auto-order today — Torture Chamber/Jinxed Choker class)
        → per-payload classification; (2) closure (i) alone insufficient (formula flips 59 Endless-
        Ranks-class + 14 src-dependent + Canopy Gargantuan) → 3 surgical extensions of ENDORSED
        machinery: T1 completion (source-independence disjunct on same-event fast path), Recipient
        reads = read-modify-write (commutation proof in-plan), census-overlap refinement of
        membership row (GroupStructure gains source_census).
      • **MATERIAL SCOPE FINDING: §5.2 category-(1) now measured NON-EMPTY — 5 named printed cards
        are provably order-DEPENDENT and auto-order today (latent CR 603.3b bug live on: Ouroboroid,
        Docent of Perfection, Sidequest: Hunt the Mark, Promise of Tomorrow, Spawn of Mayhem; proofs
        in-plan). C1 makes exactly these 5 PROMPT — user-visible, supersedes PR-6.25 §2 / PR-6.5's
        "zero printed-card reachability" premise. This is the BUG manifesting, not a regression —
        but it contradicts the mission's literal "zero printed-card behavior change" and needs
        team-lead/user adjudication (accept as rules-correct fix vs re-scope).**
      • m1-m5 all closed (citation, count, N-E pairing + on-arm doc, 9 ordering-path fixtures
        enumerated w/ per-fixture verdicts, FORGE_TEST_FULL_DB=1 zero-diff run = MERGE PRECONDITION §7).
      • New CRs (grep-verified): 603.7, 603.12, 706.2. All jq commands recorded verbatim in new §1.3.1 A-J.
- [!] **PAUSED (team-lead order, token conservation, 2026-07-02).** Reviser done + journaled above.
      STOPPED before R2 spawn per order. Idle pings suppressed. Waiting for explicit RESUME.
- [x] ADJUDICATION RECEIVED (team-lead, 2026-07-02, pre-RESUME): **(c) then (a) ADOPTED.**
      Rationale: matches user-approved C2-gating principle (outcome-invariant EXPERIENCE changes
      gated; rules-correctness FIXES ungated — C1 was the named example). C1 restores a mandatory
      CR 603.3b choice; prompting on exactly the proven-unsound set IS the fix. Option (b) rejected
      (#1 hard rule — would ship known unsoundness).
      **R2 SCOPE (execute on RESUME):**
      (1) Adversarially verify EACH of the 5 printed-card proofs (Ouroboroid, Docent of Perfection,
          Sidequest: Hunt the Mark, Promise of Tomorrow, Spawn of Mayhem) — any proof that falls
          drops that card from the visible surface; if ALL fall, literal zero-change is satisfiable
          and (a) is moot.
      (2) Adversarially verify BOTH reviser deviations: DealDamage write-Other reversal (the 100-card
          measurement) + the 3 surgical closure-(i) extensions — ESPECIALLY the source-independence
          disjunct on the T1 same-event fast path (hot-path soundness seam; hold to the inc2b
          read-guard standard).
      (3) Plan mission statement updated from "zero printed-card behavior change" to the honest
          scoped form: "zero behavior change outside the proven-unsound set; visible surface =
          exactly the CR 603.3b-unsound cards, each with an in-plan proof."
      USER CAVEAT: user retains final override at resume — if they extend C2 gating to C1's prompts,
      the plan pivots to GATED-C1 regardless of proofs. Do NOT spawn R2 until explicit RESUME.
- [x] **RESUMED (team-lead, user go-ahead, 2026-07-02).** NEW MANDATORY INPUT: PR-6.5 FINISHED +
      REBASED atop upstream/main 6cefafb21 — ALL SHAs changed. Final series: aed728539 (C0 walker),
      bec6e67b6 (C2 gated), 1d9161eec (variant-`..` hardening), 97afb8716 (inc2b detector),
      9092a8961 (granted-keyword projected-read guard test {Dethrone,Increment,Soulbond,Training}),
      bceec86e3 (item-5 loop-(iv): fire_time_conditions_read_projected_resource now scans granted-
      keyword synthesized defs via NEW pub(crate) granted_keyword_triggers_in_zone — single authority
      with trigger collection). Plan anchors @47adf7fc1 are STALE → R2 includes premise-recheck pass:
      every plan section citing PR-6.5 code (walker arms, allowlist, item-5, the 38 deferred `..`
      arms, Shambler comment fix) verified against the REBASED branch; conflict-profile work touching
      item-5/granted-keyword scanning composes with loop (iv).
- [x] R2 review DONE (2026-07-02): GAPS — 1 BLOCKER / 3 MAJOR / 6 minor, ALL NEW (in reviser's own
      extensions). Report: PR-6.75-REVIEW-R2.md. POSITIVES: all 10 R1 closures CONFIRMED genuine;
      ALL 5 printed-card proofs VERIFIED (scoped mission stands); premise-recheck clean — 42 arms
      byte-identical at tip bceec86e3 (anchors moved-only, full re-pin table in report); NO
      composition hazard with loop-(iv). Headlines: B-NEW-1 source-actor modulation hole in T1
      (lifelink/deathtouch granted state invisible to AST profile — Firebrand Archer; + player-loss
      CR 800.4c cascade channel; zero parity diff but "provably commute" would ship false);
      M-NEW-1 3 printed anaphoric-target flips (Railway Brawler TriggeringSource, Smoldering Egg/
      Replicating Ring ParentTarget→SelfRef chain); M-NEW-2 59-vs-45 floor contradiction; M-NEW-3
      header rewrite incomplete + gated-C1 caveat absent. m-4: Your Inescapable Doom = genuine 6th
      category-(1) row.
- [x] B-NEW-1 STEER: closure (a) HONEST NARROWING chosen by driver (option (b) would expand visible
      surface beyond locked adjudication → would need re-adjudication); team-lead CONFIRMED (a), no
      override, with 2 HARD REQUIREMENTS relayed to reviser mid-flight: (1) ledger row = precise
      WORK-LIST (lifelink/deathtouch per-copy modulation + Firebrand Archer witness + CR 800.4c
      loss-cascade channel + 11-card measurement); (2) STRICT PROOF-GATE on visible surface — every
      category-(1) row ships its own in-plan proof at the verified-5 standard, NO PROOF NO ROW,
      written into §5.2 as a named invariant.
- [x] R2 reviser DONE (2026-07-02): plan 403 lines, "R2 revision" header. All R2 findings closed.
      B-NEW-1: T1 "sound modulo source-actor residual" + T3 batch side scoped; side-condition block
      (lifelink/deathtouch + player-loss cascade); §6 risk 7 (1c/1d style); ledger row 5 = WORK-LIST
      (both witnesses, 12 named cards, work items a-d, Court-of-Embereth pricing); feeds-matrix
      "deliberately NO PlayerLife→SetMembership row" note. M-NEW-1: §1.3.1-K re-run (100 reproduced,
      18 anaphoric-write co-occurrences, ALL dispositioned); 2 designed rules w/ soundness args +
      fail-closed boundaries; GroupStructure.event_object_excludes_sources; N-E (v)/(vi) pairings.
      M-NEW-2 w/ MEASUREMENT-FORCED DEVIATION: def pinned EFFECT-KEYED = 57 (direct-59 had jq false
      positives); source-dependent slice = 19 NOT 14 (R1 scanned top-level targets only; recursive +
      Another + anaphoric axes add 6 cards) ⇒ floor = 38 (not the steered 45). New rows dispositioned:
      Complex Automaton → category-(1); dust stalker/thopter assembly group-unreachable (mutual CR
      603.4); scute swarm census-cleared; cactuar chain-root routing; hunger tide structure-unreachable.
      M-NEW-3: header scoped form + §0 USER-OVERRIDE caveat + §7 checkpoint. m-1..m-6 all closed
      (4,881 pinned; GLOBAL re-pin @bceec86e3 incl. anchors R2 table omitted; axis-3 wording =
      invariant STRENGTHENED (live consumer); Doom proof row w/ CR 904.3 two-copy legality; STRICT
      PROOF-GATE named invariant in §5.2 + §7 merge-precondition tie; Heaven Sent = measurement proxy;
      census-timing documented). CR DEVIATION (verified): R2 report's 800.4c was WRONG → 800.4a
      (object removal); corrected in plan header. New CRs: 702.2, 702.15, 704.5a, 707.2, 800.4a, 904.3.
      **Category-(1) table now 7 rows** (5 R2-verified + Doom + Complex Automaton — latter 2 proofs
      NOT yet adversarially verified → R3 scope).
- [x] R3 review DONE (2026-07-02): GAPS — 1 MAJOR / 1 minor / 4 nits (PR-6.75-REVIEW-R3.md).
      CONVERGING: all 10 R2 closures genuine; BOTH new proofs VERIFIED (7-row table stands); all 3
      deviations reproduced EXACT (plan's 800.4a right, R2 reviewer wrong); 5/6 dispositions sound;
      disjointness rule sound at walker-guard bar. F1 MAJOR: chain-root rule undefined on parentless-
      ParentTarget (Cactuar root Bounce{ParentTarget}, no parent ⇒ rule flips a printed auto card;
      resolver: targeting.rs:922-952 parentless ParentTarget = EVENT object for ZoneChanged / None
      for Phase; Riders of the Mark same shape, relabel kind-non-feed). F2 unit slip (38 triggers =
      37 cards). F3 Doom fallback→primary. F4 Automaton observability sentence. F5 HEAD now
      2e7ad800c (2 comment-only commits, zero drift) + designed-mechanism list omission.
- [x] R3 reviser DONE (2026-07-02): all 6 closures in, plan 404 lines, "R3 revision" header.
      F1: root-walk rule (topmost OBJECT referent) + parentless clause pinned to verified resolver
      (targeting.rs:922-952: ZoneChanged w/ object_id≠source_id ⇒ event object → rule 2; Phase ⇒
      None ⇒ pin-at-impl, classify to match, FAIL-CLOSED if unpinnable); Riders relabeled (chain-root
      class 11→10); N-E (vii) bidirectional revert-fails + Phase-arm assertion.
      DEVIATION (honest, measured): Cactuar is mode PHASE not ZoneChanged (brief was wrong) — cleared
      via the no-event-object arm (both pinnable behaviors ⇒ no sibling write touches member-private
      SelfRef predicate ⇒ commutation ⇒ AUTO matching today; fail-closed single-card prompt if
      unpinnable). Not category-(1). F2: 38 triggers = 37 cards (dup keeper of the accord), triggers-
      first form everywhere. F3: Doom fallback = PRIMARY (no selector node in AST). F4: observability
      sentence. F5: HEAD 2e7ad800c drift stated precisely (ability_scan comment-only 3394 lines +
      UNANCHORED analysis rename controller_life_never_dips→winner_life_never_dips); designed-
      mechanism list completed. No new CRs needed.
- [x] R4 confirmation DONE (2026-07-02): **CLEAN-WITH-NITS — 0 blockers / 0 majors / 0 minors /
      2 nits** (PR-6.75-REVIEW-R4.md). All F1-F5 closures genuine (re-derived from primary evidence);
      Cactuar Phase-arm re-disposition SOUND; floors reproduced; Doom/Automaton rows verified; zero
      ledger regressions; 13 CR spot-checks pass; mission 1-6 + all adjudication items ✓. Reviewer:
      "ship it to implementation."
- [x] BOTH NITS APPLIED by driver (verbatim per R4 spec — plan-doc edits, not engine code):
      nit 1 root-walk "topmost"→"nearest object-referent ancestor" (resolver-pinned; fail-OPEN on
      unprinted shapes otherwise); nit 2 (a)/(b) marked instantiations naming the resolver's other
      event-object arms (targeting.rs:924-941). R4 record line added to plan header. **PLAN FINAL:
      PR-6.75-C0FULL-C1-PLAN.md, 406 lines.** Loop trajectory: R1 1B/4M/5m → R2 1B/3M/6m →
      R3 1M/1m/4nits → R4 0/0/0/2nits (applied). Review artifacts: PR-6.75-REVIEW-R{1,2,3,4}.md +
      PR-6.75-RECON.md.
- [x] MISSION COMPLETE — final checkpoint sent to team-lead.
- [ ] (ON RESUME) If reviser completed: verify its closure report against PR-6.75-REVIEW-R1.md
      (all of B1, M1-M4, m1-m5 closed), then spawn R2 CONFIRMATION reviewer — FRESH agent,
      model fable, xhigh (ultrathink), same prompt shape as R1 (see "R1 adversarial focus" above;
      review the REVISED plan against the R1 report to confirm closures + sweep for NEW gaps;
      fresh reviewer per round, no round cap). If reviser did NOT complete or died: check plan doc
      integrity (git-less file diff vs 286-line v1 baseline; sections §0-§8 + ledger; look for
      stranded partial edits), re-read its output file if needed, respawn reviser from
      PR-6.75-REVIEW-R1.md.
- [ ] (ON RESUME, after clean round) Final plan at PR-6.75-C0FULL-C1-PLAN.md + checkpoint team-lead

## RESUME CHEAT SHEET (lossless resume point)
- Deliverable: signed-off plan at .planning/combo-detection/PR-6.75-C0FULL-C1-PLAN.md (v1 = 286
  lines; reviser editing in place — expect "R1 revision (2026-07-02)" line in status header when done).
- Pipeline state: draft done → R1 done (GAPS 1B/4M/5m, PR-6.75-REVIEW-R1.md) → reviser in flight →
  NEXT STEP = spawn R2 confirmation reviewer (fable xhigh, fresh).
- All pipeline sub-agents: model "fable", xhigh/ultrathink (user directive). Driver stays as spawned.
- Key files: PR-6.75-RECON.md (measured ground truth), PR-6.75-REVIEW-R1.md (R1 findings + steers
  given to reviser are in the reviser spawn prompt — constructive B1 close, M4 closure (i), etc.),
  this log (full history).
- Constraints unchanged: pr65-wt STRICTLY READ-ONLY (verify anchors via `git show 47adf7fc1:<path>`,
  working tree has drifted +185 lines); no engine edits; no cargo anywhere; CR greps mandatory;
  never message pr65-impl; report every round verdict to team-lead.

## PLAN v1 KEY DECISIONS (planner summary, to be adversarially verified in review)
- PRECISION PIVOT: all 42 CONSERVATIVE arms byte-identical; claim 1-bit read-precision provably can't
  reach zero printed flips (Chaotic Goo / Gutter Grime / Mana Crypt / Fruit of the First Tree = auto
  today + sibling-read-flagged + order-independent only by r/w disjointness). NEW module
  game/ability_rw.rs = kind/scope read-write conflict profile (the ":3429 precise read/write
  predicate" the deferral comment called for). ability_scan.rs untouched ⇒ axis-3 trivially identical.
- Crux: LKI freeze zones.rs:150-179 (exit, CR 603.10a/400.7); read quantity.rs:3728-3796 (zone-guarded,
  CR 608.2h); no sibling writer of lki_cache. Departed-source + departure-event reads = frozen
  constants, admitted BY DESIGN (~80 Shambler/Arcbound/Hangarback + ~50 persist/undying HadCounters).
  Live board aggregates NOT safe by co-departure (graveyard-count × return-self counterexample) ⇒
  fail-closed conflict term. Wrong EventSource comments (:3353/:3438) rewritten.
- C1: same-event = !conflict(profile) + IDENTICAL-FUNCTIONS FAST PATH (same source_id + same event ⇒
  auto unconditionally) — Demonstrate/Replicate pass structurally (share source_id = cast obj,
  seams :2547-2648, defs synthesis.rs:2318/2447); CopySpell measured sibling-clean (reads original by
  id, copy_spell.rs:20-130). Case A prompts (SRC-PT read × EXT-CTR write); revert-fail restores auto.
- C0-full: allowlist fns DELETED; 12 tags → typed carriers (legacy_batch_prompt classification) +
  frozen-read admission + conflict term. ZERO decision diffs BOTH directions (~295 allowlist-hit cards;
  widening rejected per C2-gating precedent → ledger #1). Consumer chain :3523→:3535→:3441 verified.
  zone_changes retained + doc added.
- Verification: frozen-oracle corpus parity sweep (shared_card_db pattern, FORGE_TEST_FULL_DB=1) zero-
  diff + positive floor; N-A #4269 ordering test; N-B frozen-vs-live discriminator; N-C/N-D Case A;
  N-E per-arm profile pairings; Rubblebelt C2 pins unchanged.
- OPEN: DealDamage/Mana/CreateDelayedTrigger profile kinds (sweep arbitrates); PR-6.25-era test line
  re-location at final branch. Planner observed LIVE anchor drift (inc2b landing).

## R1 adversarial focus given to reviewer
Fast-path soundness (randomness/reflexive/3+-mixed-groups/id-reuse); ability_rw.rs second-walker
hazard (drift, wildcards, add-engine-variant hook) + 4-card measurement verification; zero-diff
co-extensivity (esp. live-aggregate co-departing cards that auto today = unsound-but-shipped —
honest handling?); LKI proof verification at 47adf7fc1; CR greps; consumer completeness; anchor
reconciliation (:1336/:2088/:2091 vs :1520/:2339/:2342).
+ TEAM-LEAD ANCHORS (relayed mid-flight): (1) ability_rw.rs must mandate walker-grade discipline —
no wildcards, explicit destructures on read-bearing arms (5 fail-open `..` holes were caught in
ability_scan.rs inc1: multi_target/target_constraints/target_chooser/repeat_until/modal), add-engine-
variant hook — missing wildcard mandate ≥ MAJOR; (2) 3 open profile-kind decisions need defined
arbitration (in-PR sweep) + FAIL-CLOSED default on ambiguity.
