# PR-3 plan review — round 2 (pr3-plan-reviewer-r2)

VERDICT: CLEAN — no gaps (0 blockers, 0 high). BLOCKER 0 self-discovery + fix are SOUND; all round-1 findings RESOLVED; GameEvent surface + strict comparator untouched. 4 LOW fix-constraints for the executor (not a re-review gate).

## A) [BLOCKER 0] self-discovery + fix — SOUND
Dead-code claim TRUE (verified): fresh id per trigger triggers.rs:3894-3895; StackEntry derives PartialEq,Eq with pub id, stack is im::Vector → element-wise eq INCLUDING id (game_state.rs:4811-4817, 5518); GameState::eq compares self.stack==other.stack (7834); normalize_for_loop (7754) does NOT canonicalize stack ids → strict path compares raw ids too; project_out_resources (resource.rs:588-723) never touches s.stack today. Corroboration: trigger_event in kind is GameEvent::LifeChanged{player_id,amount} (events.rs:210-213) — no volatile id, constant amount per cycle for idx 18; idx 18 fully non-targeted (Blight-Priest "each opponent loses 1 life" + Conqueror "you gain that much life").
False-positive analysis SOUND:
- Strict comparator byte-for-byte untouched: `project_out_resources` called ONLY by `loop_states_equal_modulo_resources` (resource.rs:561-562 + tests); strict draw engine.rs:1195 calls loop_states_equal on normalize_for_loop directly — never the projection. §7 edits only project_out_resources.
- Positional canon compares ENTRY CONTENT not just zeroes ids: sets only entry.id=ObjectId(pos); kind (source_id, controller, ability, condition, trigger_event) still compared element-wise → different triggers/spells at same positions → UNEQUAL. Id is pure bookkeeping → cannot wrongly equate non-same-loop-point states.
- Cross-refs left as-is are fail-safe: stack_paid_facts (eq@7835) + pending_trigger_entry (eq@7863) holding raw ids stay raw → residual mismatch only makes UNEQUAL (suppress), never a false win. Empty/None for idx 18. stack_trigger_event_batches absent from eq (field@6804, not in eq body 7822-7968), inserted only for len>1 triggers.
- Still distinguishes different abilities at same position: YES (kind carries boxed ResolvedAbility + condition + trigger_event, compared verbatim).
- Step-0 proof real & discriminating: halt (ResolutionHalted) WITHOUT §7, modulo-match + GameOver{P0} WITH §7.
BLOCKER 0 VERDICT: SOUND.

## B) Round-1 findings — RESOLVED
- [BLOCKER 1] firewall RESOLVED. player_has_cant_lose@sba.rs:308 private fn (pub(crate) bump correctly acknowledged); player_has_cant_win@static_abilities.rs:955 already pub; pub mod sba@game/mod.rs:127, pub mod static_abilities@:136. Firewall calls BOTH on raw cycle_end.
- [BLOCKER 2] indices RESOLVED. Live array (index=sequence−1): 17=Sanguine+Exquisite (line 238, gated_on None, LethalDamage); 18=Blight-Priest+Conqueror (245, None, LethalDamage); 19=Niv (252, None but correctly excluded — "any target"⇒SelectTargets break + dual library-faller). idx 2=Doc Aurlock gated_on Some (136, would fail assert); idx 1=Kilo already driven (1929). confirmed_drivers_match_expected@1947 asserts gated_on.is_none(). Promote 17/18.
- [HIGH] all-living-players gate RESOLVED. priority_auto_pass_decision@engine.rs:354 Passes both UntilStackEmpty(:363) and UntilEndOfTurn(:374) w/o meaningful check; cap-path priority_player_has_meaningful_action@:453 (current holder only) used@:1128. New no_living_player_has_meaningful_priority_action probes each living player (sets waiting_for=Priority{p}); legal_actions keys off state.waiting_for (ai_support/mod.rs); has_meaningful_priority_action@ai_support/mod.rs:723 pub. All-players generalization correct (session-masked victim needn't hold priority at match iter). Negatives L-neg-A (sessionless⇒Exit-arm break@:1151) + L-neg-B (session-masked⇒gate declines) specified & discriminating.
- [MEDIUM] panic RESOLVED. map_delta@resource.rs:516-534 inserts only if d!=0; §8 uses .get(&k).copied().unwrap_or(0) for every read.
- [LOW] drive seam/redundant/L4 RESOLVED. SetAutoPass{UntilStackEmpty} pattern real (engine.rs:991-1015); detect_loop re-runs modulo@loop_check.rs:144 so dropping step-1 correct; L4 reframe honest.

## C) Fresh adversarial pass
- GameEvent surface byte-identical (reuses GameOver{Some}+WaitingFor::GameOver, no new variant; engine-inventory.json identical).
- L4 "live strict-draw of a cascade impossible" TRUE (normalize_for_loop preserves raw fresh ids → loop_states_equal never holds for a cascade; §7 doesn't touch strict; single-faller guard ⇒ can only WIN never draw; net-zero ⇒ None ⇒ existing halt). Insertion-order invariant (strict runs first & returns) + U-draw make "modulo never hijacks a draw" discriminating.
- detect_loop sig (cycle_start,cycle_end,&delta,controller,mandatory)@loop_check.rs:135 matches §8; classify_win_kind LethalDamage on delta.life[pid]<0 && pid!=controller (255-260). §8 uses raw cycle_end for firewall (so static eval isn't perturbed by normalize_for_loop's layers_dirty) — correct.

## NEW findings — all LOW (executor fix-constraints; no re-review gate)
1. [LOW] CR 810.8a is a Two-Headed Giant TEAM rule (docs/MagicCompRules.txt:6733) but the firewall restricts to living.len()==2 (strict 2-player, non-team). 2-player can't-win/can't-lose is CR 101.2 + 104.2b/104.3b (correctly cited). FIX: drop CR 810.8a from §8 firewall comment, or scope to deferred multiplayer/team only.
2. [LOW] §7 comment enumerates only cross-refs to raw stack ids, not volatile content INSIDE kind. trigger_event/subject_match_count/die_result (game_state.rs:5298-5333) are part of kind and ARE compared by the modulo comparator. Verified stable for idx 18 (LifeChanged no volatile id; subject_match_count/die_result None) so §7 sufficient. FIX: Step 0 must explicitly confirm these in-kind fields byte-stable across two same-phase cycle points (if any volatile, §7 alone ⇒ dead code: fail-safe for soundness but non-vacuity fails, win never fires). Add to §7 "left as-is is fail-safe" enumeration.
3. [LOW] perf: §10 find_map calls live_mandatory_loop_winner per window element (≤128) every iter after 32, no cheap fingerprint pre-filter (life-fingerprint changes each cycle). Mitigated: heavy detect_loop projection behind cheap delta guards; true loops match within ~2 cycles; bounded by caps (window 128, max_iters ≤10_000). FIX (optional): one-line worst-case note (large-board monotone-drain board-changing cascade = O(window×snapshot)/iter up to cap).
4. [LOW] doc-debt: live_mandatory_loop_winner (analysis::loop_check) now calls game::sba::player_has_cant_lose, and detect_loop runs in the reducer → falsifies module doc loop_check.rs:12-16 ("purely offline… never called from the reducer"). Plan §11 commits to the update. FIX: confirm loop_check.rs:12-19 doc update lands in the same change.

## Executor scope note (from the planner's caveat)
BLOCKER 0 expands PR-3's scope to the shared modulo comparator (project_out_resources, PR-2 code) → the implementation gate MUST include the full `analysis::` suite as a no-regression check. Without §7 PR-3 is inert.
