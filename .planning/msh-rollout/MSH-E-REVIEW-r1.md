# MSH-E plan review — round 1 (mshe-plan-reviewer-r1)

VERDICT A (Ruinous coverage-only): CHANGES REQUIRED — BLOCKER (honesty guard regresses 2 GREEN cards).
VERDICT B (Hawkeye feature): CHANGES REQUIRED — diagnosis sound, but every code seam mis-cited; B2 assumes an AST shape that doesn't exist.

> NOTE (orchestrator): plan + this review were done against base 5eca83b8c. Main is now v0.8.0 (ac1917cf5) and MSH-F merges on top. The round-2 revision happens AFTER MSH-E is rebased onto v0.8.0+MSH-F — RE-VERIFY all cited line numbers there (they will shift). The findings below are logic, base-independent.

## VERIFIED RIGHT (measured)
- Support-flip mechanism CONFIRMED: `check_silent_drops` (coverage.rs:5117-5135) fires `effective_oracle>effective_parsed`; `count_effective_oracle_lines` (5371) folds bullets only when `is_modal_header_line` (5432-5458) recognizes header; list lacks "choose up to x"/"that many". Hawkeye 5>4, Ruinous 6>4. `parse_warning_gap_label` (5198) maps SwallowedClause{detector}→missing (no support-predicate change needed). `check_swallowed_clauses` (swallow_check.rs:78) early-returns on any_ability_has_unimplemented (93), builds ast_json (103).
- Ruinous runtime REALLY works (NO false green): all 4 modes + dynamic_max_choices:{Ref:CostXPaid}. Destroy-token FilterProp::Token=>obj.is_token (filter.rs:3179); Sacrifice player_scope:All APNAP per-player rebind (test sacrifice.rs:1064); cost_x_paid finalize_cast (casting_costs.rs:5506-5508)→CostXPaid (quantity.rs:2837-2843); modal max clamp modal_choice_for_player (ability_utils.rs:571-573). Caveat: coverage `missing` not in export → "only SilentDrop flags it" verified indirectly (all parse_details supported, gap_count 0, detect_dynamic_qty doesn't fire, handlers present).
- Hawkeye AST defects CONFIRMED: modal min1/max1/count3, NO dynamic_max_choices. Inventory *Paid/*Count = AdditionalCostPaymentCount,CostXPaid,KickerCount only → new TimesCostPaidThisResolution passes existence+categorical gate.
- CR fidelity ALL VERIFIED: 603.12a verbatim = the Hawkeye rule (pay multiple times → reflexive fires ONCE); 700.2/b/d, 107.3m, 601.2b, 701.26, 701.21, 119.3, 122.1, 608.2c resolve+match. WhenYouDo eval effects/mod.rs:6885-6887.

## SUB-PLAN A — BLOCKER
- **[BLOCKER] A1 — detector regresses 2 GREEN cards.** Marker "choose up to that many"/"choose up to x" also matches NON-modal "choose up to X/that many <nouns>" SELECTION clauses. Measured across the 10-card marker class: **Temporal Firestorm** (supported TRUE, gap_count 0, modal_nodes 0 — "choose up to X creatures, where X is times kicked") → plan detector FIRES → GREEN→RED REGRESSION; **Heroic Feast** (supported TRUE, gap_count 0, modal_nodes 0 — "choose up to that many target creatures") → FIRES → REGRESSION. (Discordant Dirge/Reap Intellect/Suppression Ray red modal_nodes 0; Bumi/Riku red modal_nodes 1; Tranquil Frillback red modal_nodes 1 = exact Hawkeye pattern; Ruinous modal_nodes 1 dynamic_max Some → green; Hawkeye modal_nodes 1 no dynamic_max → red.) **Required:** detector must ALSO require a parsed modal node — fire only when ast_json contains `"modal":{` AND lacks `"dynamic_max_choices":{` AND oracle has the dynamic header. Reviewer verified this gate fully discriminating across all 10 (Temporal Firestorm/Heroic Feast/Discordant/Reap/Suppression → silent; Hawkeye/Frillback/Bumi/Riku → red; Ruinous → green). ADD the missing hostile test: non-modal "choose up to X <nouns>" (no modal node) → detector silent.
- **[MEDIUM] A2 — `"dynamic_max_choices":null` introspection branch is dead.** ability.rs:12807 `#[serde(default, skip_serializing_if="Option::is_none")]` → field omitted when None (Hawkeye), emitted when Some (Ruinous). Key on ABSENCE of `"dynamic_max_choices":{`, not a `:null` form.
- **[LOW] A3 — `is_modal_header_line` bare "choose up to x" is loose** (matches selection clauses); benign today (the 5 non-modal matches have no following bullets, no card flips from the recognizer change alone — verified) but fragile. Prefer the em-dash header form. The recognizer change alone does NOT cause A1; the detector does.

## SUB-PLAN B — diagnosis correct, seams wrong
- **[HIGH] B1 — cited repeat/flag seams are the WRONG function; Hawkeye traverses neither.** `drive_repeat_for_outermost` (effects/mod.rs:3972, plan cited 3991) only runs with player_scope/unless_pay (gate repeat_for_outermost_with_scope_or_unless, 3962); Hawkeye has neither. REAL path: (1) up-front optional gate at **5403** fires one OptionalEffectChoice, stashes ability, returns; (2) on Accept, resolve_optional_effect_decision (1740) strips optional, re-enters, Fixed-count loop runs via the **repeated_full_chain branch at 5800-5868** (repeat_for.is_some() && sub_ability.is_some() → resolve_ability_chain(full_chain_iteration) at 5859 re-resolves PayCost AND the modal per iteration). Claim 3b right but lives at 5854/5859, not 3991. Per-iteration flag clear (5078) is the player_scope path, not Hawkeye's. B3 must target repeated_full_chain.
- **[HIGH] B2 — the Fixed-count path has NO per-iteration optional handling.** Current AST: optional:true on OUTER def (one up-front yes/no), then repeat_for:Fixed(3) runs inner unit 3× MANDATORILY. Only kind_driven/member_driven branches fire per-iteration OptionalEffectChoice (5836-5865); plain Fixed-count applies optional once up front (comment 5848-5853). So B's "in repeat_for driver, optional PayCost: clear flag/increment/decline-stops-early" requires (a) parser to MOVE optionality onto the per-iteration unit (B4) AND (b) NEW Fixed-count per-iteration-optional+early-stop driver logic mirroring kind/member. That new driver is the real work and is only implied — scope it FIRST-CLASS or B under-delivers "pay {1} up to three times" (becomes "pay once-or-not, then 3× forced").
- **[MEDIUM] B3 — flag-carryover mis-located.** cost_payment_failed_flag set 5005, cleared 5078 (player_scope only). In Hawkeye's repeated_full_chain loop never cleared per iteration → if real, the fix is at 5800-5868, not 3988-4008. Tie to the new per-iteration-optional driver.
- **[LOW] B4 — 601.2b** is modal announcement-during-casting; "illegal mode can't be chosen" is more precisely 700.2e. Acceptable pairing.
- Sound on B: 603.12a grounding exact + load-bearing; modal_choice_for_player reuse spine real; K∈{0,2,3} + "Explosive once, not 2×K" discriminators non-vacuous IF aimed at 5854; interactive/MP/AI/frontend "reuse, no new surface" holds.

## BOTTOM LINE
- A: fix A1 (require `"modal":{` node in detector) → clean + airtight (Ruinous green / Hawkeye+Frillback red, ZERO green regressions across the 10-card class). A2/A3 cleanups.
- B: thesis correct, CR work excellent; re-point B2/B3 at repeated_full_chain (5800-5868) + up-front optional gate (5403); scope the new Fixed-count per-iteration-optional+early-stop driver as explicit work. No fundamental redesign; split is right once A1 lands.
