# MSH-F plan review — round 1 (mshf-plan-reviewer-r1)

VERDICT A (Cosmic Cube): CHANGES REQUIRED. VERDICT B (Hawkeye): CHANGES REQUIRED (minor).
Reviewed against actual code in wt-msh-f @ feat/msh-f-cosmic-cube-hawkeye (off 5eca83b8c).

## Specimen & anchor accuracy — CONFIRMED ("no new variant" thesis holds)
- Aetherworks Marvel parses to `Dig{6,keep_count:0,Any}`→`CastFromZone{ExiledBySource,without_paying:true,Cast}`→`PutAtLibraryPosition{ExiledBySource,0,Bottom}` (it is Activated, not a trigger — plan reuses only the chain, correctly).
- STRONGER THAN CLAIMED: Cosmic Cube ALREADY parses today to a `YouAttack` trigger wrapping that identical chain with `CastFromZone.constraint:null`, `without_paying:true`, `optional:true`. Rabble Rousing is an independent `YouAttack`+`CastFromZone{ExiledBySource}` precedent. Only the MV ceiling is dropped.
- Artist's Talent → `Plus{value:2}` (serialized bare `"value":2`) + `Typed{controller:You}` + `PlayerOrPermanentsControlledBy{Opponent}` + `NoncombatOnly` + a `ClassLevelGE{3}` Hawkeye lacks.
- Anchors confirmed: `CastPermissionConstraint::ManaValue{comparator, value:QuantityExpr}` (ability.rs:2188-2191, value ALREADY QuantityExpr); `QuantityRef::Aggregate` (4132); resolver Plus arm `amount.saturating_add(value)` (replacement.rs:949, exhaustive match); `resolve_quantity(&GameState,&QuantityExpr,PlayerId,ObjectId)` (quantity.rs:68); `replacement_source_player` (replacement.rs:51); custom QuantityExpr Deserialize bare-int→Fixed, recursive (ability.rs:5013).
- Serde back-compat TRUE: `{"type":"Plus","value":2}`→`Plus{Fixed{2}}`. QuantityExpr derives Eq → field-lift compiles against DamageModification's #[derive(Eq)].
- Live-power read CONFIRMED: `QuantityRef::Power{scope}`→`resolve_object_pt(...,|obj| obj.power,...)` (quantity.rs:1697), same path as SetToSourcePower. `parse_cda_quantity("~'s power")→Ref(Power{Source})` locked by test `cda_quantity_self_power`. Mid-batch power-change test constructible.
- CR text all grep-clean: 614.1a, 508.1/508.1b, 202.3, 208.1, 120.1/120.3, 107.1b, 109.4. Coverage honestly marks both cards supported:false, gap_count:1.

## SUB-PLAN A — Cosmic Cube
- **[HIGH] "delegate to broadest existing aggregate entry" does NOT compose on real input.** The aggregate branch in `parse_quantity_ref_with_context` only returns `Aggregate` when the type-phrase consumes the WHOLE remainder: `snapshot_ok = remainder.trim().is_empty() || parse_cast_snapshot_suffix(...)` (oracle_quantity.rs:300-310). Cosmic Cube tail = "the greatest power among attacking creatures you control without paying its mana cost"; after filter consumes "attacking creatures you control", remainder=" without paying its mana cost" → snapshot_ok=false → branch doesn't fire → `parse_quantity_ref(full_tail)` returns None. Constraint parser receives full untrimmed `rest` (mod.rs:15367; `without_paying` scan at 15365 doesn't strip it). REQUIRED: isolate the quantity substring with nom (`take_until(" without paying")`) before delegating; add a verification case proving the trailing suffix doesn't defeat quantity parsing.
- **[MEDIUM] shared `parse_mv_comparator` factoring conflates two positions.** Existing Beseech comparator is a POST-value suffix — value first (mod.rs:15621), then `tag("or less")`/`tag("or greater")` (15623-15629). Cosmic Cube's is a PRE-value prefix ("less than or equal to N"). One combinator can't sit in both positions; new branch defines its own prefix-comparator alt. (GT/LT/GE/LE/EQ all exist.)
- **[MEDIUM] row-2 enforcement seam mis-stated.** `cast_permission_constraint_allows_cast` (casting.rs:1592-1598) resolves the dynamic value via `resolve_quantity(state, value, obj.controller, obj.id)`, binds obj.controller=caster="you" correctly — ceiling IS enforced. But that branch returns true (permissive) when resulting_mv is None and (unlike Fixed branch 1586-1590) has NO printed-MV fallback. Dynamic ceiling enforced at cast FINALIZATION, not necessarily filtered from the initial offer. REQUIRED: reframe row-2 to assert finalize-time rejection; confirm what resulting_mv the offer path supplies.
- **[LOW] Provenance "Consumer: cast_from_zone.rs:357" wrong path.** Cosmic Cube's `from among them` arm hard-codes `driver: LingeringPermission` (mod.rs:15503) → constraint consumed by `grant_lingering_permissions` (cast_from_zone.rs:491-568, threaded into `CastingPermission::ExileWithAltCost{constraint}` at 556/568), not the during-resolution branch at 357. Runtime test must target the lingering path.
- **[LOW] Line drift:** `FilterProp::Attacking` is ability.rs:2591 (plan says 2538); `ObjectProperty::Power` variant 4606 (plan cites 4605). Cosmetic.
- **[LOW] CR 601.2e** is the cast-legality rule; resulting-MV computation is CR 601.2f. Consider pairing 601.2f.

## SUB-PLAN B — Hawkeye, Young Avenger
- **[MEDIUM] never acknowledges structurally-identical sibling `QuantityModification::Plus{value:u32}`** (ability.rs:15826, "Modeled after DamageModification"). Lifting DamageModification::Plus→QuantityExpr while leaving this u32 widens a deliberate parallel — sibling-cluster smell. Defensibly out of scope (counters/tokens = different event+CR section; categorical-boundary rule supports separation) BUT plan must (a) state this as explicit non-goal with rationale, and (b) disambiguate the blast-radius grep — `QuantityModification::Plus` also lives in oracle_replacement.rs (5737/6292/6308/12704), so a naïve `Plus {` grep conflates them. File-level enumeration (replacement.rs, oracle_replacement.rs, add_target_replacement.rs, oracle_trigger.rs) matches the 4 DamageModification::Plus files; compiler-enforced so no site silently missed.
- **[LOW→MEDIUM] Scope (Plus-only): APPROVED — redirect the deferred follow-up.** Plus-only is right. The genuine parameterize-don't-proliferate candidate is `SetTo{value}` + `SetToSourcePower` (ability.rs:15794/15800) → lifting SetTo.value collapses SetToSourcePower into `SetTo{Ref(Power{Source})}`. Minus is genuinely independent. Reframe follow-up as SetTo/SetToSourcePower unification (still out of scope now).
- **[LOW] New parser arm two foot-guns:** (1) must precede `value(Plus{0}, tag("plus x"))` in `parse_that_much_damage_offset` (oracle_replacement.rs:4942) or the freeze arm shadows it — plan ordering right. (2) Hawkeye text ends "...~'s power." — `parse_cda_quantity("~'s power")` is tested WITHOUT trailing period; confirm `.` tolerance/strip, and note `parse_cda_quantity` returns Option (needs `map_opt` to compose in the nom alt). Position-independent `scan_damage_modification` (oracle_replacement.rs:4918) cleanly handles Hawkeye's LEADING "instead"; antecedent filters parse identically to Artist's Talent.
- **[LOW] Name the regression-guard migrations.** Artist's Talent assertions at oracle_replacement.rs:11783/11810 (and 14026/14036) hard-assert `Plus{value:2}`/`Plus{value:0}`; the lift forces them to `Plus{value:Fixed{...}}`. Name these.
