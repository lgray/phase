# S25-effect-verb-bespoke — FINAL implementation plan (40 Standard-legal cards)

> **✅ SIGNED OFF — 2026-07-01. Reviewed clean by `/review-engine-plan` (2 planner↔reviewer rounds; final review returned "no blocking gaps found").**
> Round 1: fixed one load-bearing blocker — the `ManaSpendRestriction` mechanism already exists (enum `types/ability.rs:1695`, parser + payment threading built; a wrong-term grep had falsely claimed "no restricted-mana mechanism"). Round 2 re-framed B8 accordingly (measured: parser already lowers Tin Street Gossip + Overgrown Zealot per `mana.rs:3808-3823`; the real gap is runtime `PaymentContext::SpecialAction(TurnFaceUp)` emission, not parsing) and fixed two citation/verification notes (B3 `:79`, B5 watcher already exists). Card accounting: A=14 + B=12 + C=14 = **40**, zero missing/duplicate.
> **Residual non-blocking assumptions (resolve at each card's `/engine-implementer` dispatch, not gating):** (1) B8's exact `game/morph.rs` turn-face-up cost-emit site not line-confirmed (mirrors live `UnlockDoor`); the `FaceDownSpell` branch stays honest-red until a face-down cast routes its cost. (2) A8 Rhys variable-count counter-*removal* path not line-confirmed (may nudge Rhys A→B). (3) Group C internal-block existence (C3/C4/C5/C7) is deferred to each card's own `/review-engine-plan` gate by design — classification set, exact reuse decided at the gate. C9's CR was corrected to 118.7 (cost reduction) in this pass.

**Snapshot:** 2026-07-01 @ `acd2f5e6b` · card-data + coverage-data regenerated (inputs stamp `2026-07-01T20:05Z`).
**Backing artifact:** `.planning/coverage-analysis/out/standard/cluster-assignment.tsv` (rows tagged `S25-effect-verb-bespoke`, exactly 40 cards, 0 unclustered — verified by `grep -Pc "\tS25-effect-verb-bespoke$"`).
**Skill:** produced via `/engine-planner`. Seeded from the reviewed draft `.planning/coverage-analysis/S25-PLAN.md` (round-1 `/review-engine-plan` pass); this FINAL resolves all four "Open items to resolve at dispatch" by measured grep, applies the resulting regrouping, and satisfies every engine-planner mandatory section. Each family names its execution skill (`/oracle-parser`, `/add-engine-effect`, `/add-trigger`, `/add-replacement-effect`, `/add-engine-variant`, `/review-engine-plan`).

> **Hard rule (inherited from `std-rollout/PLAN.md`): all 40 cards MUST be implemented — no deferrals, no "out-of-scope," no dropping.** Groups A/B/C order cards by *implementation cost*, not by whether they ship. "Group C" = harder cards *gated* by `/review-engine-plan` (reviewed before code) and then built — every one ships. `/review-engine-plan` gating is the mechanism for shipping hard cards correctly; it is NOT a synonym for deferral.

---

## 0. What changed vs the seed plan (measured corrections)

Every claim below was grep-confirmed against `crates/engine/src/**` and `docs/MagicCompRules.txt` this pass. The seed plan's round-1 review left four open items "to resolve at dispatch, do not guess now." They are now resolved by measurement, and the resolution moved four families:

| # | Seed classification | Measured fact | Final classification |
|---|--------------------|----------------|---------------------|
| A7 mana-spend | all 3 cards "one `ManaSpendPermission` axis, parser-only" | Two *distinct* existing mechanisms. **(1) Cast-time spend permission:** `ManaSpendPermission::AnyTypeOrColor` (`types/ability.rs:2274`) — Outrageous Robbery ("spend mana as though it were mana of any type"). Parser fills it; parser-only. **(2) Restricted produced-mana (CR 106.6):** `enum ManaSpendRestriction` **ALREADY EXISTS** (`types/ability.rs:1695`) — a mature parameterized enum (`SpellOnly`, `SpellType`, `ChosenCreatureType`, `SpellTypeOrAbilityActivation`, `ActivateOnly`, `ActivateTagged`, `XCostOnly`, `SpellWithManaValue { comparator, value }`, `FaceDownSpell` :1788, `TurnPermanentFaceUp` :1799, `Any(Vec<..>)` :1802, …). Its parser `parse_mana_spend_restriction` (`parser/oracle_effect/mana.rs:1491`) **already lowers both cards** — measured: tests at `mana.rs:3808-3823` assert Tin Street Gossip → `Any([FaceDownSpell, TurnPermanentFaceUp])` and Overgrown Zealot → `TurnPermanentFaceUp`. Payment threading exists (`mana_payment.rs:3402`, `mana_sources.rs:887 restrictions: &[ManaSpendRestriction]`). The seed's "no engine mechanism / 0 hits" was a **wrong-term grep** (`RestrictedMana`/`spend_restriction`); the correct term `ManaSpendRestriction` is a large existing surface. The **only** residual is runtime liveness: `has_payable_branch` (`ability.rs:1828`) gates both cards red because no site emits `PaymentContext::SpecialAction(TurnFaceUp)`/face-down-cast yet (only `UnlockDoor` is live). | Outrageous Robbery → **A** (parser-only, `AnyTypeOrColor`). Tin Street + Overgrown → **B8**, redefined: NOT a new axis — a small **runtime payment-site wiring** (emit `PaymentContext::SpecialAction(TurnFaceUp)` from the morph turn-face-up path, reusing the existing `pay_special_action_mana_cost` / `UnlockDoor` precedent). No new variant, no parser change. |
| A5 Parker Luck | "`Effect::LoseLife` + revealed-MV `QuantityRef`, parser-only" | Oracle: "two target players **each** reveal the top card… **each lose life equal to the mana value of the card revealed by the other player**." This is a *symmetric two-player cross-reveal* with each player's loss keyed to the **other's** revealed card — not a self-referential revealed-MV ref. No such cross-binding exists. | → **B9** (paired-reveal + cross-referenced event-context MV). |
| A2 Vraska | "Animate + granted ability + lose-all-types; move to C if the granted-ability lowering isn't a block" | The "**it's a `<type>` artifact with '`<ability>`', and it loses all other card types**" sub-pattern EXISTS as `become_copy_except.rs::parse_its_a_type_with_ability` (`crates/engine/src/parser/oracle_effect/become_copy_except.rs:712`; Shelob / Espers to Magicite / Myrkul) → `SetCardTypes` + quoted-ability grant + `AddType`/`AddSubtype`. BUT it lives only in the **copy** path; Vraska applies it to a **returned/reanimated non-copy** permanent (`return that card to the battlefield tapped under your control`). The type/ability/lose-types piece is reusable; the reanimate+apply composition (gated by optional pay-{1}) is new. | → **C** (reviewed before code), reusing the Shelob helper. Not a new subsystem — a new composition. |
| A9 Sarkhan (behold) | already reclassified A→B by round-1 review | Grep confirms **no `Effect::Behold`**: only `AbilityCost::Behold` + `BeholdCostAction` (`types/ability.rs:6974`, `6483`). Sarkhan's "you may behold a Dragon" is an ETB **effect**. | → **B7** (effect-side behold wrapper reusing the `BeholdCostAction` reveal/choose primitive). |

Net accounting after corrections: **Group A = 14, Group B = 12, Group C = 14 = 40.** Group *membership* is unchanged by the B8 correction — Tin Street Gossip and Overgrown Zealot were already in Group B; only B8's *rationale* changes (existing-enum runtime wiring, not a new axis). Full per-card list in §Scope Matrix and §Card Accounting.

---

## Analogous trace (hard gate)

**Traced feature: Cloak** — the exemplar of "the `Effect` variant + resolver already exist; only the parser can't lower the phrasing," which is Group A's entire shape. Full path parser → type → resolver → tests, each grep-confirmed:

- **Parser dispatch:** `crates/engine/src/parser/oracle_effect/imperative.rs:8190` — `"cloak" | "cloaks"` match arm → builds `ImperativeFamilyAst::Cloak { target, count }` (currently only accepts `tag("cloaks the top ")`, imperative.rs:8195). Lowered at `imperative.rs:10007` → `Effect::Cloak { target, count }`. Keyword-action registration: `parser/oracle_util.rs:1466` (`KEYWORD_ACTIONS = &["manifest dread", "cloak", "manifest"]`).
- **Type:** `types/ability.rs:10668` `Effect::Cloak { target, count }` (CR 701.58a annotation present); `EffectKind::Cloak` at `13182`.
- **Resolver:** `game/effects/cloak.rs`, dispatched from `game/effects/mod.rs`; face-down/printed-characteristics interplay in `game/printed_cards.rs`; coverage registration in `game/coverage.rs`.
- **Tests:** parser round-trip in `parser/oracle_effect/tests.rs`.

Every Group A family follows this exact shape: **extend the parser dispatch/lowering only**; the `Effect` variant, resolver, and coverage registration already exist. The Cloak parser today handles `"cloak the top N"` but **not** the article/pronoun phrasings Vannifar (`"Cloak a card from your hand"`) and Expose the Culprit (`"shuffle that pile, then cloak them"`) use — that gap generalizes to the whole group.

**Second trace (for the type-set-with-ability composition used by Vraska/C):** `parse_its_a_type_with_ability` at `parser/oracle_effect/become_copy_except.rs:712` → `ContinuousModification::{AddType, AddSubtype}` (`types/ability.rs:17317`) + `SetCardTypes` replacement + quoted-ability grant (shares `parse_it_has_quoted_ability`). CR 205.1a (set card type) + CR 707.9d (copy-CDA interaction) + CR 205.1b ("in addition to its other types").

---

## GROUP A — Parser-only, reuse an existing `Effect` variant (14 cards; batch first, cheapest ROI)

Skill: **`/oracle-parser`** for every family. No new `Effect` variant, no resolver change, no coverage-registration change — the variant, resolver, and registration already exist (grep-confirmed below). Each is a nom-combinator extension in the relevant `oracle_effect/`, `oracle_static/`, or `oracle_trigger.rs` dispatcher. **Nom compliance:** extend the existing `alt()`/`tag()` dispatch in the named file — never `contains()`/`starts_with()`/`find()` for dispatch. Detection = `parse_*(text).is_some()`.

| A# | family / building block (grep-confirmed to exist) | cards | swallowed clause → target lowering |
|----|--------------------------------------------------|-------|-----------------------------------|
| **A1** | **Cloak** → `Effect::Cloak` (`types/ability.rs:10668`; parser arm `imperative.rs:8190`) | Vannifar, Evolved Enigma · Expose the Culprit | `"Cloak a card from your hand"` / `"shuffle that pile, then cloak them"` — extend the cloak arm's `alt()` to accept `a`/`one`/`them` + optional zone-qualified source (`from your hand`) beside the existing `tag("cloaks the top ")`. Expose composes `Effect::Shuffle` + `Effect::Cloak` in sequence (`oracle_effect/sequence.rs`). |
| **A2** | **Animate / add-type / "in addition to its other types"** → `Effect::Animate` (`9291`) + `ContinuousModification::AddType` (`17317`) | Brilliance Unleashed · The Tomb of Aclazotz | Brilliance = plain `Animate` ("it's a 3/3 … creature with flying"). Tomb adds `AddType`/`AddSubtype` for "is a Vampire in addition to its other types" (CR 205.1b) + enters-with-counter composition. Both blocks exist; parser lowering only. |
| **A3** | **Color-set bound to event-context mana color** → `ContinuousModification::SetColor` (`17411`; parser `animation.rs:215`) | Foraging Wickermaw | `"{1}: Add one mana of any color. This creature becomes that color until end of turn."` — `SetColor` exists but takes fixed `colors: Vec<ManaColor>`. "that color" = the color chosen when the mana was added → bind `SetColor.colors` to the event-context mana color (provenance, see §Identity). Parser + a light event-context color binding; no new `Effect`/resolver. |
| **A4** | **Dig + LoseLife** → `Effect::Dig` (`8675`) + `Effect::LoseLife` (`8426`) | Stargaze | `"Look at twice X cards… put X into hand, rest into graveyard. You lose X life."` — compose `Dig { look=2X, keep=X, rest→graveyard }` + `LoseLife { X }` in sequence. Both variants exist; parser sequence only. |
| **A6** | **RemoveKeyword (compose two)** → `ContinuousModification::RemoveKeyword` (`17260`) | Quick Draw | `"Creatures target opponent controls lose first strike and double strike until end of turn."` — lower "lose `<kw>` and `<kw>`" to **two** `RemoveKeyword` mods (compose per-keyword via `alt()`, never a bespoke "lose-fs-and-ds" variant). Controller scope = opponent (see §Scope Matrix). |
| **A7** | **Cast-time spend-as-any** → `ManaSpendPermission::AnyTypeOrColor` (`2274`) | Outrageous Robbery | `"you may spend mana as though it were mana of any type to cast it"` → the existing `AnyTypeOrColor` on the cast permission. Parser fills `mana_spend_permission: Some(AnyTypeOrColor)`. **Only Outrageous Robbery** — the "spend this mana only…" restrictions are B8, not here. |
| **A8** | **Counters (put/remove, dynamic X)** → `Effect::{PutCounter, PutCounterAll}` (`9194`-region) + `QuantityRef::ObjectCount` (`4288`) | Crowd-Control Warden · Rhys, the Evermore · Esper Terra · Prishe's Wanderings | Warden: put X +1/+1 where X = `ObjectCount { filter = other creatures you control }`. Rhys: "Remove any number of counters" (variable-count removal — verify `Effect::RemoveCounter` variable path). Esper Terra: "put up to three lore counters" + "Add {W}{W}…" (counters + mana-add sequence). Prishe: reflexive "when you search your library this way, put a +1/+1 counter" (delayed/reflexive trigger + counter → `/add-trigger` wiring, no new effect). |
| **A10** | **RepeatContinuation** → `RepeatContinuation` (`game/effects/mod.rs:693,5226`) | Another Round | `"Exile any number of creatures you control, then return them…, then repeat this process X more times."` — lower the exile+return sequence, wrap with the repeat continuation, count `X`. Pure parser. |
| **A11** | **Excess-damage reflexive trigger + targeted Destroy** → `SimpleEvent::DealtExcessDamage` (`oracle_trigger.rs:8392`) + `Effect::Destroy` | Rhino's Rampage | `"When excess damage is dealt to the creature an opponent controls this way, destroy up to one target noncreature artifact with mana value 3 or less."` — reuse the S01 excess-damage trigger (`DealtExcessDamage`, parser arm `oracle_trigger.rs:8499`) + a targeted `Destroy` with the MV≤3 + noncreature-artifact filter. "this way" ties the trigger to the damage this spell dealt (see §Identity). Parser + reflexive-trigger wiring; no new effect. CR 120.10. |

*(A5 and A9 from the seed are now B9 and B7 — see §0.)*

**Group A subtotal: 14 cards.** Batch-1 dispatch: one `/engine-implementer` run per 1–2 families, **sequential** — shared files `oracle_effect/imperative.rs`, `oracle_effect/sequence.rs`, `oracle_static/animation.rs` are collision points → do NOT parallelize A1/A2/A3/A8/A10.

---

## GROUP B — Small new engine effect / variant / composition (12 cards; `/add-engine-effect` · `/add-trigger` · `/add-engine-variant`)

| B# | family / new building block | cards | notes / CR (grep-verified) |
|----|-----------------------------|-------|-----------|
| **B1** | **Multi-zone search-by-name + exile** (new effect; `SearchLibrary` exists but not gy+hand+library at once) | Deadly Cover-Up · The End | `"search its owner's/controller's graveyard, hand, and library for any number of cards with that/the same name and exile them. That player shuffles, then draws a card for each card exiled from their hand this way."` One reusable "search these three zones for name-matches, exile, then draw-per-hand-exile" effect (the *Bell, Book and Candle* / *Conspiracy* class). CR 701.23 (Search) + CR 201.2a / CR 201.3a (same/interchangeable name) + CR 701.13 (Exile). 2 cards, 1 block. Name latched at resolution (see §Identity). |
| **B2** | **Delayed sacrifice at a future end step** (reuse delayed-trigger builder: `game/effects/delayed_trigger.rs`, `DelayedTriggerCondition` `types/ability.rs:2461`, `create_warp_delayed_trigger` `game/stack.rs:2521`, `epic_upkeep_trigger` `game/effects/epic.rs:75`) | Kav Landseeker · Choreographed Sparks | `"At the beginning of the end step on your next turn, sacrifice that token"` / copy gains `"At the beginning of the end step, sacrifice ~"`. Compose the existing delayed-trigger builder. Choreographed also grants haste + the delayed sac to a **copy** (compose with the copy effect). CR 603.7 (delayed triggered abilities). |
| **B3** | **Grant abilities beyond activated** (parameterize `GainActivatedAbilitiesOfTarget`) | Symbiote Spider-Man | `"It gains this card's other abilities."` Resolver `game/effects/gain_activated_abilities.rs:79` filters `.filter(\|a\| a.kind == AbilityKind::Activated)` — **activated-only**. Symbiote's "other abilities" includes a **triggered** combat-damage ability. **Parameterize** the variant with a typed ability-kind selector (e.g. `granted: GrantedAbilityScope { ActivatedOnly \| AllOther }`) — NOT a sibling. Run `/add-engine-variant`. CR 611.2c (grant set fixed at effect start). |
| **B4** | **CDA token with dynamic base P/T** (extend token P/T; aggregate exists) | The Skullspore Nexus | `"create a green Fungus Dinosaur creature token with base power and toughness each equal to the total power of those creatures."` The aggregate exists: `QuantityRef::Aggregate { function: AggregateFunction, property: ObjectProperty }` (`types/ability.rs:4452`) / `QuantityExpr::Aggregate` (`5262`-region) with `ObjectProperty::Power` + `AggregateFunction::Sum` — "total power of those creatures" = `Aggregate { Sum, Power, filter = death-event set }`. BUT the token spec P/T is fixed: `power: Option<i32>` / `toughness: Option<i32>` (`types/ability.rs:8080/8083`, mirror at `17193/17194`). **Extend** the token P/T to accept a `QuantityExpr` (CDA path, CR 604.3 + CR 208.4b) resolved at creation and installed as a characteristic-defining base P/T. Run `/add-engine-variant` for the token-spec field change. "those creatures" = the death-event set (provenance). |
| **B5** | **"first `<spell type>` you've cast this turn" trigger** (`/add-trigger`) | Alania, Divergent Storm | The watcher **already exists** (measured): `state.spells_cast_this_turn_by_player` (`game/day_night.rs:27,188`), `QuantityRef::SpellsCastThisTurn { scope, filter }` (`game/quantity.rs:383,2483`) — the `filter` field already supports spell-type scoping — and `game/game_object.rs:942` already implements a "first qualifying spell each turn" grant gated by `SpellsCastThisTurn == 0`. B5 is therefore **add a spell-type filter + first-cast (`== 0`) intervening-if to the existing watcher**, NOT build a new one. Sibling of S19 new-trigger matchers. |
| **B6** | **Control-loss trigger + unattach** (`/add-trigger`) | Stolen Uniform | `"When you lose control of that Equipment this turn, … unattach it."` — lose-control trigger matcher + unattach effect. CR 603.6 (zone/loss triggers) + CR 702.6 (Equip / attach). "that Equipment" provenance = the specific Equipment (see §Identity). |
| **B7** | **Behold as an ETB effect** (new effect wrapping `BeholdCostAction`) | Sarkhan, Dragon Ascendant | `"you may behold a Dragon. If you do, create a Treasure token."` No `Effect::Behold` exists (only `AbilityCost::Behold`/`BeholdCostAction`, `types/ability.rs:6974`). Add a small effect-side behold that reuses the same reveal-from-hand / choose-on-battlefield primitive (`BeholdCostAction`), then gate the "create Treasure" on success (`if a Dragon was beheld`). CR 701.4a/b. |
| **B8** | **Restricted produced-mana — RUNTIME liveness** (reuse existing `ManaSpendRestriction` enum + `pay_special_action_mana_cost`; NO new variant, NO parser change) | Tin Street Gossip · Overgrown Zealot | `"Add {R}{G}. Spend this mana only to cast face-down spells or to turn creatures face up."` / `"Add two mana of any one color. Spend this mana only to turn permanents face up."` **The `ManaSpendRestriction` enum, its parser, and payment threading all already exist** (see §0): `parse_mana_spend_restriction` lowers Tin Street → `Any([FaceDownSpell, TurnPermanentFaceUp])` and Overgrown → `TurnPermanentFaceUp` (tests `mana.rs:3808-3823`). The **only** gap is runtime: `has_payable_branch` (`ability.rs:1828-1842`) returns `false` for both `FaceDownSpell` and `TurnPermanentFaceUp` because no production payment site emits `PaymentContext::SpecialAction(TurnFaceUp)` — so the surrounding `Effect::Mana` line honestly lowers to `Effect::Unimplemented` (coverage red by design). To ship: wire the morph/disguise turn-face-up cost through the **existing** `pay_special_action_mana_cost(state, player, src, cost, SpecialAction::TurnFaceUp, events)` helper (`game/casting.rs:11567`), exactly as the live `UnlockDoor` special action does (`casting.rs:11563-11592`, `game/engine.rs:143`). Then flip `has_payable_branch` for `TurnPermanentFaceUp` (and, once a face-down cast routes its cost through `PaymentContext::Spell`, `FaceDownSpell`). CR 106.6 + CR 116.2b + CR 702.37e. **Extension, not creation** — no `/add-engine-variant`. 2 cards, 1 runtime block. |
| **B9** | **Symmetric two-player reveal + cross-referenced MV loss** (new composition) | Parker Luck | `"two target players each reveal the top card… each lose life equal to the mana value of the card revealed by the other player. Then they each put the card they revealed into their hand."` Needs a paired reveal that binds each player's `LoseLife` to the **other** player's revealed-card MV (`ObjectProperty::ManaValue` exists; the cross-binding is new), then a per-player hand-put. Compose reveal + `LoseLife { QuantityExpr = MV of other's revealed card }` + return-to-hand. |

**Group B subtotal: 12 cards.** B3/B4 hit `/add-engine-variant` (B8 does NOT — it reuses the existing `ManaSpendRestriction` enum + `pay_special_action_mana_cost` helper). Each is one small engine effect, trigger matcher, parameterization, or (B8) runtime payment-site wiring, then parser lowering where applicable. Frontend: verify whether B7 (behold choose) and B8 (which permanent to turn face up) introduce a `WaitingFor` choice; if so run `/add-interactive-effect` (behold reuses the existing cost-side choice UI; B8 reuses morph's existing turn-face-up flow).

---

## GROUP C — New subsystem / genuinely bespoke (14 cards; `/review-engine-plan` BEFORE code — none deferred)

| C# | card | why heavy / new subsystem |
|----|------|---------------------------|
| **C1 — HEAVIEST** | Secret of Bloodbending · Bumi, Unleashed | **Controlling Another Player (CR 723 — verified `docs/MagicCompRules.txt:6157`) does not exist at all.** "You control target opponent during their next turn / combat phase." Bumi pairs a combat restriction ("only land creatures can attack") onto a controlled combat. A turn/priority-ownership subsystem → dedicated `/review-engine-plan` gate, then shipped. The single hardest S25 card; implemented, not dropped. |
| **C2** | Moonlit Meditation | Token-creation **replacement** ("first time you would create one or more tokens each turn, instead create that many copies of enchanted permanent") → `/add-replacement-effect` + once-per-turn latch. CR 614 (replacement) + CR 616. |
| **C3** | Niko, Light of Hope | "Shards you control become copies of it until the next end step" — `Effect::BecomeCopy` exists (`types/ability.rs:9164`) but the **mass** (dynamic set) + **duration-bound** form needs verification/extension. CR 707 + CR 611.2c. |
| **C4** | Memory Vessel | Play-permission from an exile set + "can't play cards from your hand" — a play-permission/-restriction pair. CR 118 / CR 601. |
| **C5** | No Witnesses | "Each player who controls the most creatures investigates. Then destroy all creatures." — most-of aggregate over players (S16-adjacent per-player enumeration; `QuantityRef::PlayerCount` + `ObjectCountBySharedQuality` are adjacent) + `Effect::Investigate` (`8807`) + wrath. |
| **C6** | Graceful Takedown | Multi-target fan-out: "any number of target enchanted creatures … and up to one other target … each deal damage equal to their power to target creature you don't control." Heavy targeting state-machine (flagged Tier-3 in std-rollout). Gap tag `multi_source_compound_each_power_damage`. |
| **C7** | Glen Elendra's Answer | Mass ability-removal / negate over "all abilities your opponents control" — needs exact Oracle context at dispatch; likely a static mass ability-removal (CR 613). |
| **C8** | Vincent's Limit Break | Three creature-token faces (Galian Beast / Death Gigas / Hellmasker at `{0}/{1}/{3}`) — modal/level token-face subsystem. Weakest "class" claim (~1 card); at dispatch, either reuse the modal/level-token-face block or record it as a justified single-card consumer (still ships). |
| **C9** | The Dominion Bracelet | "This ability costs {X} less to activate, where X is ~'s power" — dynamic cost reduction keyed to source power. Note Skullspore B4 has the *spell*-side "{X} less where X is greatest power" (`This spell costs {X} less`); C9 is the *activated-ability* cost-reduction-by-quantity sibling — sequence after B4 lands so it inherits the quantity plumbing. CR 118.7 (cost reduction; verified `docs/MagicCompRules.txt:990` — *not* 118.9, which is alternative costs). |
| **C10** | Vanille, Cheerful l'Cie | "Fearless l'Cie — you may pay {3}{B}{G}" alt-cost keyword — route through the alt-cost parser once the S23/S26 alt-cost cluster lands. CR 118 / CR 601.2b. |
| **C11** | Sandman, Shifting Scoundrel | "target land card from your graveyard" — target-in-graveyard `parse_target` zone extension (S17-adjacent). Small but a targeting-layer change → reviewed. |
| **C12** | Bre of Clan Stoutarm | "Otherwise …" else-branch (conditional-else parse) — S12-adjacent; small but structural (modal/else). |
| **C (Vraska)** | Vraska, the Silencer | **Moved from A2 (see §0).** `"return that card to the battlefield tapped under your control. It's a Treasure artifact with '{T}, Sacrifice this artifact: Add one mana of any color,' and it loses all other card types."` Reuse the Shelob type-set-with-ability helper (`become_copy_except.rs:712`, `SetCardTypes` + quoted-ability grant + lose-all-types) but apply it to a **reanimated non-copy** permanent (`ChangeZone`→battlefield, tapped, under controller), gated by the optional `pay {1}` trigger cost. New composition, not new subsystem. CR 205.1a + CR 707.9d + CR 603.6. |

**Group C subtotal: 14 cards.** C1 is the only true new-subsystem heavyweight (CR 723). Several (C9, C10, C11, C12) are small once their adjacent cluster (S23/S26 alt-cost, S17 target-in-gy, S12 else) lands — sequence them *after* those clusters so they inherit the block. Every one is implemented, none dropped.

---

## Mandatory architectural sections

### Pattern Coverage
The cluster is 40 Standard cards; the *families* cover classes far beyond these 40. A1 Cloak → every article/pronoun cloak phrasing (Bloomburrow + future). A6 → every "loses `<kw>` and `<kw>`". A8 → every dynamic-X / variable-count counter placement/removal. B1 → the whole graveyard+hand+library name-exile class (Bell Book and Candle, Conspiracy, Declaration in Stone family). B2 → every delayed end-step sacrifice. B3 → every "gains `<object>`'s abilities" beyond activated-only (Quicksilver Elemental generalization). B4 → every CDA token with dynamic base P/T (Ferocious Werefox, Hangarback-style). B8 → every restricted-produced-mana card (CR 106.6 — a recurring template). C1 → the entire CR 723 control-another-player rules area. **The only weak (≤1-card) "class" is C8 (Vincent's Limit Break)** — explicitly flagged; it ships under the no-defer rule, and at dispatch either reuses the modal/level-token-face block or is recorded as a justified single-card consumer. No family is a silent single-card special case.

### Building Blocks
**Reused (measured to exist, file:line):**
`Effect::Cloak` (`ability.rs:10668`) · `Effect::Animate` (`9291`) · `Effect::Dig` (`8675`) · `Effect::LoseLife` (`8426`) · `Effect::Investigate` (`8807`) · `Effect::BecomeCopy` (`9164`) · `Effect::SearchLibrary` (`9435`) · `Effect::GainActivatedAbilitiesOfTarget` (`9194`) · `Effect::{PutCounter, PutCounterAll, MultiplyCounter}` (`9194`-region) · `RepeatContinuation` (`game/effects/mod.rs:693,5226`) · `ContinuousModification::{RemoveKeyword (17260), SetColor (17411), AddColor (17414), AddType (17317), AddSubtype}` · `ManaSpendPermission::AnyTypeOrColor` (`2274`) · `ManaSpendRestriction` enum (`ability.rs:1695`; variants incl. `FaceDownSpell 1788`, `TurnPermanentFaceUp 1799`, `Any 1802`) + parser `parse_mana_spend_restriction` (`mana.rs:1491`) + payment threading (`mana_payment.rs:3402`, `mana_sources.rs:887`) + `pay_special_action_mana_cost` (`casting.rs:11567`) · `AbilityCost::Behold` + `BeholdCostAction` (`6974`, `6483`) · `QuantityRef::SpellsCastThisTurn { scope, filter }` (`quantity.rs:383,2483`) + `spells_cast_this_turn_by_player` watcher (`day_night.rs:27,188`) · `QuantityRef::{ObjectCount (4288), Aggregate (4452), ObjectCountBySharedQuality (4315), PlayerCount (4322), CountersOn (4343)}` · `QuantityExpr::Aggregate` (`5262`-region) · `ObjectProperty::{Power, ManaValue}` (`4948`-region) · `AggregateFunction` (`4085`-region) · delayed-trigger builder (`game/effects/delayed_trigger.rs`, `DelayedTriggerCondition ability.rs:2461`, `create_warp_delayed_trigger stack.rs:2521`, `epic_upkeep_trigger epic.rs:75`) · `SimpleEvent::{DealtExcessDamage (oracle_trigger.rs:8392), DealtExcessNoncombatDamage (8390)}` · `parse_its_a_type_with_ability` (`become_copy_except.rs:712`) · `oracle_effect/sequence.rs`, `oracle_effect/imperative.rs`, `parse_target`, `parse_for_each_clause`, `parse_quantity_ref`.

**New (justified, ≥2-card class or whole missing rules area):** B1 multi-zone name-search-exile effect (2 cards); B3 `GrantedAbilityScope` parameterization of an existing variant; B4 dynamic-CDA token P/T field (`QuantityExpr` in place of `Option<i32>`); B9 cross-player reveal composition; C1 Controlling-Another-Player subsystem (CR 723); C2 token-create replacement latch. Every new *variant* runs the `/add-engine-variant` gate against `cargo engine-inventory`. **(B8 is NOT new — the `ManaSpendRestriction` enum + parser + payment threading already exist; B8's residual is runtime payment-site wiring reusing `pay_special_action_mana_cost`, so it is an Extension.)**

### Logic Placement
- **Group A = parser only** (`crates/engine/src/parser/…`). The resolver/effect/coverage layers are untouched because each `Effect` variant already exists (grep-confirmed). A3 adds a light event-context color binding still inside the parser/lowering (`SetColor.colors` bound from the mana-add event) — no resolver change.
- **Group B = new/parameterized `Effect`/trigger** in `types/ability.rs` + `game/effects/…` + `game/effects/mod.rs` dispatch + `game/coverage.rs` registration, then parser lowering. **B8 is purely runtime and adds no new type/parser:** the `ManaSpendRestriction` enum, its parser, and its payment threading already exist; B8 emits `PaymentContext::SpecialAction(TurnFaceUp)` from the morph turn-face-up path (`game/morph.rs` / `game/casting.rs`) via the existing `pay_special_action_mana_cost` helper and flips `has_payable_branch` (`ability.rs:1828`) — a rules constraint on spending that lives entirely in the engine's payment layer, never the parser. B4's CDA install lives in the token-creation resolver.
- **Group C = same as B plus a subsystem** (turn/priority ownership for C1; replacement layer for C2; targeting-layer zone extension for C11).
- **Frontend:** none for Group A. Verify per Group-B/C card whether a new `WaitingFor` choice is introduced (B7 behold reuses the existing cost-choice UI; B8 face-up target may reuse morph's existing turn-face-up flow; C1 control-another-player needs priority-ownership UI — flagged at its review gate).

### Rust Idioms
Typed enums throughout, never bools: A7/B8 use `ManaSpendPermission` (cast-time spend-as-any) / the existing `ManaSpendRestriction` enum (restricted-purpose produced mana — already parameterized with `FaceDownSpell`/`TurnPermanentFaceUp`/`Any`), never a bool. B3 uses a typed `GrantedAbilityScope` (ActivatedOnly / AllOther), not an `include_all: bool`. A5→B9 / A8 / B4 use `QuantityExpr`/`QuantityRef`, not inline ints. A6 composes two `RemoveKeyword` mods (one per keyword via `alt()`), never a bespoke "lose-first-and-double-strike" variant. B4 binds token base P/T to a `QuantityExpr` (CDA) rather than a magic sentinel. Exhaustive `match` on `Effect`/`EffectKind` (no wildcard) so the compiler flags every new variant's unhandled arm at `game/effects/mod.rs`, `game/coverage.rs`, `game/printed_cards.rs`, `game/trigger_index.rs`.

### Nom Compliance (mandatory — parser files change)
Every Group A/B detection extends an existing `alt()`/`tag()` dispatch in the named module:
- A1 Cloak: extend the `alt()` inside the `"cloak" | "cloaks"` arm at `imperative.rs:8190` — add `alt((tag("cloak a "), tag("cloak one "), tag("cloak them"), tag("cloaks the top ")))` plus an optional `preceded(tag("cloak "), parse_zone_source)` for `from your hand`. Never `contains("cloak")`.
- A2/A3: extend `oracle_effect/animation.rs` (`SetColor` at `:215`, add-type path). **NB:** `animation.rs:31` currently uses `lower.contains(" loses all other card types ")` — that is a *pre-existing* nom violation in code I am not touching; my A2 add-type extension composes per-axis (`alt` of core-type × subtype) and does not add new `contains()`.
- A6: `alt()` of keyword tags, applied twice via a `separated_list`/`preceded(tag(" and "), …)` composition, each → one `RemoveKeyword`. Compose per-axis, never enumerate "first strike and double strike" as one tag.
- A8/A10/A11: extend the counter dispatch, the repeat-continuation lowering (`oracle_effect/sequence.rs`), and the excess-damage trigger `alt()` at `oracle_trigger.rs:8499` respectively.
- B1/B7/B9: new detection is a `parse_*(text).is_some()` combinator added to the relevant dispatcher, never a substring scan. (B8 adds NO parser detection — `parse_mana_spend_restriction` already lowers both cards, tests `mana.rs:3808-3823`; B8 is runtime-only.)
Detection is `parse_*(text).is_some()`, never a `contains()`/`starts_with()`/`find()` dispatch. Each `/engine-implementer` sub-plan MUST name the **exact combinator** for every new detection — hard gate at sub-plan review.

### Extension vs Creation
**Extension** (new parser arm / parameterize existing / reuse existing infra): all of Group A, plus B2 (delayed-trigger builder), B3 (parameterize `GainActivatedAbilitiesOfTarget`), B5 (add spell-type filter to the existing `SpellsCastThisTurn` watcher), B6 (trigger matcher), B7 (reuse `BeholdCostAction`), **B8 (runtime payment-site wiring reusing the existing `ManaSpendRestriction` enum + `pay_special_action_mana_cost`)**, C-Vraska (reuse Shelob helper), C9/C11/C12 (inherit adjacent-cluster blocks). **Genuine creation** (justified by ≥2-card class or a whole missing rules area): B1 (multi-zone search-exile effect), B4 (dynamic-CDA token P/T), B9 (cross-player reveal), C1 (CR 723 subsystem), C2 (token-create replacement).

### Analogous Trace
Named and traced above (Cloak: `parser/oracle_effect/imperative.rs:8190` → `types/ability.rs:10668` → `game/effects/cloak.rs` → `game/effects/mod.rs` → `parser/oracle_effect/tests.rs`; plus the type-set-with-ability composition `become_copy_except.rs:712`). Both full paths listed.

### Variant Discoverability
Before any new/changed enum surface — B1 (new `Effect`), B3 (`GrantedAbilityScope`), B4 (token P/T `QuantityExpr`), B9 (any new `QuantityRef` cross-binding), C1/C2 — run `cargo engine-inventory` (gitignored; regenerate locally) and the `/add-engine-variant` checklist; grep the inventory for sibling-cluster smells (e.g. a `Search*` family so B1 parameterizes rather than adds a naked sibling; a `GainAbilities*` cluster so B3 parameterizes). **B8 needs NO gate:** the `ManaSpendRestriction` enum already exists (`ability.rs:1695`) and already parameterizes the produced-mana-restriction axis (`FaceDownSpell`, `TurnPermanentFaceUp`, `Any`, `SpellType`, …); B8 adds no variant, only runtime liveness. `ManaSpendPermission` (cast-time permission) and `ManaSpendRestriction` (produced-mana restriction) are separate CR mechanisms that already coexist in the codebase — no unification question to resolve.

### Verification Matrix (approach; per-card matrix built at `/engine-implementer` dispatch)
For each family: **(1)** a parser round-trip test asserting the new phrasing lowers to the exact `Effect`/mod (e.g. Vannifar → `Effect::Cloak { count: 1, target: <hand-source> }`; Quick Draw → two `RemoveKeyword`); **(2)** a **runtime** test (GameScenario + GameRunner::cast().resolve(), `card-test` skill) that the card resolves with a measured board delta — mandatory for every "engine already resolves this" claim, a shape test alone does NOT satisfy support; **(3)** coverage flips `supported=false→true` in `out/standard/` on re-scan (`coverage-breakdown.sh --format standard`); **(4)** a hostile/negative sibling **per seam**: A6 — a card that *gains* a keyword must NOT hit the lose-arm; A7/B8 — spend-as-any vs spend-restricted-purpose must not collapse (two-permission fixture, first branch reached = the enum match arm); B3 — a donor with only a triggered ability must actually transfer it under `AllOther` but NOT under `ActivatedOnly` (multi-authority: two ability kinds); B1 — a card with a *different* name in the searched zone must NOT be exiled (CR 201.2a boundary); B9 — asymmetric MV (each player loses the *other's* MV, proven by unequal reveals); **(5)** revert-failing assertion (remove the new parser arm / variant → the round-trip + runtime test fails). **Deferred-semantics honesty:** if any card's Oracle text is accepted before its subsystem exists mid-development, it MUST stay red via `Effect::unimplemented(name, fragment)` (`types/ability.rs`) — a transient in-progress guard, never a shipping state (see [[unimplemented-to-supported-exposes-unwired-path]]). No card is counted cleared until its semantics resolve correctly.

### Identity / Provenance Contract (mandatory per-card for provenance cards)
Each below pins: source phrase → authority type & id/value → binding time → live-vs-latched → storage → consuming function → invalidation → multi-authority hostile fixture.

- **Rhino's Rampage — "excess damage… *this way*"**: authority = the specific damage event this spell dealt; bind at the spell's damage resolution; latched to that event id; consumed by the `DealtExcessDamage` reflexive trigger; invalidated after the trigger resolves. Hostile: a *second* unrelated excess-damage event same turn must NOT re-fire this trigger.
- **Prishe's Wanderings — "search *this way*"**: authority = the search event this effect performed; latched at resolution; consumed by the reflexive put-counter; hostile: an unrelated library search does not add the counter.
- **Deadly Cover-Up / The End — "cards with *that name* / the same name"**: authority = the name of the exiled/targeted card, **latched at resolution** (CR 201.3a); stored as the matched name; consumed by the multi-zone search filter; invalidated after the effect. Hostile: a same-name card entering a searched zone *after* latch is still matched by the latched name (name is a value, not a live pointer); a differently-named card is NOT exiled.
- **Foraging Wickermaw — "become *that color*"**: authority = the mana color chosen when the ability's mana was added; bind at mana-add; stored on the `SetColor` mod's `colors`; live for the "until end of turn" duration. Hostile: adding a *different* color next activation sets a different color; SetColor from a prior activation is a separate timestamped effect.
- **Niko — "copies of *it* until the next end step" (C3)**: authority = Niko's characteristics at effect start (CR 611.2c set fixed); duration-bound to next end step; consuming = `BecomeCopy` mass application. Hostile: Niko changing after the effect begins does not change the already-fixed copy set.
- **Kav Landseeker — "sacrifice *that token*" (B2)**: authority = the specific token id created; latched into the delayed-trigger condition; consumed at the future end step; invalidated if the token already left the battlefield (CR 603.6). Hostile: a look-alike token must NOT be sacrificed.
- **Stolen Uniform — "lose control of *that Equipment*" (B6)**: authority = the specific Equipment object id; consumed by the lose-control trigger; hostile: losing control of a *different* Equipment does not fire.
- **Symbiote Spider-Man — "*this card's* other abilities" (B3)**: authority = Symbiote's ability list snapshot at grant (CR 611.2c); stored on the recipient; hostile: Symbiote gaining a new ability after the grant does not retroactively grant it.
- **Vraska — "*that card*… *this artifact*" (C)**: authority = the returned card's object id (new object post-zone-change, CR 603.6); the granted sac ability's "this artifact" self-references the returned permanent. Hostile: two dying creatures in one window each get their own returned-object binding.
- **Secret of Bloodbending — "*that player*" (C1)**: authority = the target opponent; control transferred during their next turn (CR 723.1); hostile: a second control effect / a player who skips a turn.

---

## Scope Matrix (heavies + controller-scope families)
Per round-1 review item 6, these enumerate reachable scope variants + negative-sibling tests at dispatch:
- **C1 (control another player, CR 723):** scope axes = which player controlled (target opponent), which turn/phase (next turn vs next combat phase), priority ownership during control. Bumi adds a combat-restriction scope ("only land creatures can attack"). Negative siblings: player with an extra turn, a player who won't take another turn, multiplayer (control passes to the correct controller). CR 104.4 multiplayer correctness.
- **C6 (multi-target fan-out):** target-slot scope = "any number of target enchanted creatures" (0..n) × "up to one other target" (0..1) × "target creature you don't control" (damage sink). Negative: 0 sources chosen → no damage; illegal-target pruning per CR 608.2b.
- **A6 (RemoveKeyword on opponent-controlled creatures):** controller scope = "target opponent controls" (not yours, not all). Negative sibling: a card that removes keywords from *your* creatures must not route through the opponent-scoped arm; a card that *grants* keywords must not hit the remove arm.
- **B3 (grant scope):** ability-kind scope = ActivatedOnly vs AllOther. Negative: `ActivatedOnly` must not transfer a triggered ability; `AllOther` must transfer it.
- **B8 (restricted mana purpose):** restriction scope = {cast face-down spells, turn creatures face up} (Tin Street) vs {turn permanents face up} (Overgrown). Negative: the restricted mana must NOT pay for an unrestricted spell; unrestricted mana must still pay normally (two-mana-source fixture).

---

## Dispatch order (sequential; shared parser files are collision points)
1. **Group A parser batch (14 cards)** — biggest ROI, zero engine risk. Order to avoid `imperative.rs`/`sequence.rs`/`animation.rs` collisions: A1 Cloak → A4 Dig+LoseLife → A6 RemoveKeyword → A3 SetColor → A7 spend-as-any → A8 counters → A2 Animate/add-type → A10 RepeatContinuation → A11 excess-damage. One `/engine-implementer` run per 1–2 families; re-scan `coverage-breakdown.sh --format standard` after each to confirm the flip.
2. **Group B (12 cards)** — B1 multi-zone search (2 cards, 1 block) → B2 delayed-sac → B7 behold-effect → B8 restricted-mana (2 cards, 1 block) → B3/B4 (variant gate) → B5/B6 triggers → B9 cross-reveal.
3. **Adjacent-cluster inheritors** — C11 (S17 target-in-gy), C12 (S12 else), C10 (S23/S26 alt-cost), C9 (cost-reduction-by-qty, after B4) *after* their sibling clusters land so they reuse the block.
4. **Group C heavies — each `/review-engine-plan`-gated before code, then built (none deferred)** — C-Vraska (reuse Shelob helper) → C2 replacement → C3 Niko → C5 No Witnesses → C6 fan-out → C7 Glen Elendra → C8 Vincent → C4 Memory Vessel → **C1 Controlling-Another-Player (CR 723) last** (largest new subsystem; implemented, not dropped).
5. Re-run `cluster-assign.sh standard` after each batch; S25 must shrink monotonically toward **0** and no cleared card may reappear in `unsupported.tsv`.

---

## Card Accounting (must equal 40 — verified)
**Group A (14):** Vannifar Evolved Enigma · Expose the Culprit · Brilliance Unleashed · The Tomb of Aclazotz · Foraging Wickermaw · Stargaze · Quick Draw · Outrageous Robbery · Crowd-Control Warden · Rhys the Evermore · Esper Terra · Prishe's Wanderings · Another Round · Rhino's Rampage.
**Group B (12):** Deadly Cover-Up · The End · Kav Landseeker · Choreographed Sparks · Symbiote Spider-Man · The Skullspore Nexus · Alania Divergent Storm · Stolen Uniform · Sarkhan Dragon Ascendant · Tin Street Gossip · Overgrown Zealot · Parker Luck.
**Group C (14):** Secret of Bloodbending · Bumi Unleashed · Vraska the Silencer · Moonlit Meditation · Niko Light of Hope · Memory Vessel · No Witnesses · Graceful Takedown · Glen Elendra's Answer · Vincent's Limit Break · The Dominion Bracelet · Vanille Cheerful l'Cie · Sandman Shifting Scoundrel · Bre of Clan Stoutarm.

**14 + 12 + 14 = 40. Zero missing, zero duplicated** (cross-checked against the sorted 40-name list from `cluster-assignment.tsv`). **Clearance target: all 40 ship — zero deferrals.** Group A (14) + Group B (12) = 26 land on existing/small engine work; Group C (14) follows via `/review-engine-plan`, with exactly one genuinely new subsystem (C1, CR 723) and the rest small adjacent-cluster inheritors or compositions — every one implemented, none dropped.

---

## CR verification ledger (all grep-confirmed in `docs/MagicCompRules.txt` this pass)
701.58 / 701.58a Cloak (3779/3781) · 701.4 Behold (3293) · 723 Controlling Another Player (6157) · 120.10 excess damage (1138) · 603.6 zone-change triggers (2597) · 603.7 delayed triggered (2610) · 702.6 Equip (3933) · 701.23 Search (3463) · 201.2a same name (1290) · 201.3a interchangeable names (1299) · 701.13 Exile (3378) · 106.6 restricted-spend mana (425) · 604.3 CDA (2667) · 208.4b base P/T (1527) · 611.2c continuous-effect set fixed at start (2909) · 205.1a set card type (1398) · 205.1b "in addition to its other types" (1400) · 707.9d copy-CDA interaction (5654) · 116.2b turn-face-up special action (900) · 704.5a life-loss SBA (5492). No CR corrections were needed to the seed's already-fixed set; all new CRs cited here were verified before writing.

## Residual assumptions (could not be fully closed by grep; resolve at each card's dispatch)
- **B8 runtime liveness (Tin Street, Overgrown)** — the `ManaSpendRestriction` enum, parser, and payment threading are confirmed to exist; the residual is a runtime wiring: the morph/disguise turn-face-up special action must route its cost through the existing `pay_special_action_mana_cost(.., SpecialAction::TurnFaceUp, ..)` (`casting.rs:11567`) so `has_payable_branch` (`ability.rs:1828`) can go live for `TurnPermanentFaceUp`. The exact morph turn-face-up cost-emit site in `game/morph.rs` was not line-confirmed this pass; whether that path already computes a payable cost is resolved at B8 dispatch (it mirrors the live `UnlockDoor` emit at `engine.rs:143` / `casting.rs:11563-11592`). The `FaceDownSpell` branch stays honest-red until a face-down cast routes its `{cost}` through `PaymentContext::Spell`.
  <br>*(B5 Alania is NO LONGER a residual: the `SpellsCastThisTurn { scope, filter }` watcher + `spells_cast_this_turn_by_player` + the `== 0` first-cast gate at `game_object.rs:942` are all confirmed present — B5 only adds a spell-type filter to it.)*
- **A8 Rhys** — variable-count counter *removal* ("remove any number") path existence not line-confirmed; if the removal effect only takes a fixed count, A8 gains a small variable-count parameter (would nudge Rhys toward B, still ships).
- **C3/C4/C5/C7** internal-block existence deferred to their `/review-engine-plan` gate by design (Group C is reviewed-before-code); classification (subsystem vs composition) is set, exact reuse decided at the gate.
